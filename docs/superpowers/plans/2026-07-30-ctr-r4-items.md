# CTR Racing Mode — R4 (Items) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** The CTR item loop — boxes → roulette → missile / shield / turbo / beaker — usable by player and AI on both tracks, per the racing spec's R4 phase. Opens with the R4-BINDING spin-out fix recorded in the spec's debts.

**Architecture:** Pure-logic cores (ItemSlot state machine with injectable RNG, homing solver, shield timer) + thin nodes (ItemBox Area3D, Missile/Beaker Node3D), all tuning in a new `&"items"` section. Hits route through the EXISTING `apply_spin_out()` once Task 1 makes it drift-safe. AI item decisions extend AiDriver's state dict. Seam-safe target selection via SpineFollower totals only.

## Global Constraints

Same as R3's (numeric literals 0/1/-1 in `src/racing/**`; tuning provably live incl. migration; TDD failing-first; suites green each commit — GUT 1006 / Py 247 / 5 lints baseline, grep raw GUT for 'Parse Error', known flakes incl. the now-documented pre-existing `test_island_slice` stale-slide flake — re-run isolated; stage explicit paths; never stage the two other-agent WIP docs; feel gates human-only). Seam ruling: any "kart ahead/behind" query uses SpineFollower totals. AI must use items through the same routing as the player (no privileged paths).

---

### Task 1: Spin-out/drift-FSM binding fix + carried bookkeeping

**Files:** `src/racing/kart/kart_controller.gd` (apply_spin_out cancels the drift FSM via the existing `cancel_slide()`, and `boost_tap()`/`hop_pressed()` gate on `is_spinning_out()` — add the motor query proxy if missing), `src/racing/kart/kart_motor.gd` (expose `is_spinning_out()` if absent), tests (drift/controller: mid-slide spin-out ends slide + zeroes accrued boost + blocks taps during stun; recovery after `spin_out_duration_s` restores normal control), spec debts (strike debt #1; add shared race/time-trial `track_id` best-times debt), `tests/racing/test_race_session.gd` (tighten the graybox 20s sustained test's escape valve: the respawn OR-branch must ALSO require final total ≥ the healthy floor × a documented recovery fraction, not merely wedge+4m).
- [ ] Failing tests → fix → suites → commit.

### Task 2: ItemTuning section + ITEM input surface

**Files:** `src/tuning/item_tuning.gd`, `data/tuning/racing/items.tres`, tuning_service/gameplay_tuning/gameplay.tres wiring + validation + migration (5th new section — follow precedent exactly), export-verifier sync; TouchControls racing layout gains the ITEM button (the R1-deferred placeholder — reuse spin-button metrics, racing layout only), gamepad ITEM mapping (B/circle), RaceSession routes ITEM presses to the player kart's item slot (Task 3 API — land the routing behind a has-item no-op until Task 3).
**ItemTuning exports:** `roulette_duration_s=1.2`, `roulette_tick_s=0.12`, `box_respawn_s=4.0`, `box_pickup_radius_m=1.6`, `missile_speed_mps=26.0`, `missile_turn_rate_degrees_per_s=140.0`, `missile_lifetime_s=6.0`, `missile_arm_delay_s=0.25`, `missile_hit_radius_m=1.2`, `shield_duration_s=6.0`, `turbo_boost_s=1.6`, `beaker_arm_delay_s=0.7`, `beaker_lifetime_s=25.0`, `beaker_hit_radius_m=1.1`, `ai_item_use_cooldown_s=1.5`, `ai_missile_max_target_gap_m=45.0`. Validation strictly positive.
- [ ] Failing tuning tests → implement → suites → commit.

### Task 3: ItemSlot state machine + ItemBox (pure core + node)

**Files:** `src/racing/items/item_slot.gd` (RefCounted: states empty/rolling/held/using; `start_roll(rng_value: float)` — deterministic injection, maps to one of the four items uniformly; `tick(delta)` advances the roll using roulette fields; `held_item() -> StringName` (&"missile"/&"shield"/&"turbo"/&"beaker"/&"none"); `use() -> StringName` returns-and-clears), `src/racing/items/item_box.gd` + `scenes/racing/item_box.tscn` (Area3D pickup, hides + respawns after `box_respawn_s`, body filter = karts only, graybox visual), session wiring: box body_entered → that kart's ItemSlot roll (player + AI identical path).
- [ ] Failing tests (roll determinism given rng values, roulette timing, single-held semantics, use clears, box respawn cycle in a scene test) → implement → suites → commit.

### Task 4: The four items (cores + nodes + hit routing)

**Files:** `src/racing/items/missile.gd` + scene (homing: target = nearest kart AHEAD by SpineFollower totals at launch — session provides a targets snapshot Callable; steering = pursuit with `missile_turn_rate` cap toward the target kart's position, arm delay, lifetime, hit radius → target.`apply_spin_out()` unless shield-blocked), `src/racing/items/beaker.gd` + scene (dropped at launcher's position minus a kart length, arms after delay, static hazard, hit → spin_out unless shielded), shield: timed flag ON THE KART (`kart_controller.set_shielded(duration)` + `is_shielded()`; a blocked hit consumes the shield early — document), turbo: `apply_boost(turbo_boost_s)` immediately. Item USE routing in RaceSession: player ITEM press or AI decision → slot.use() → spawn/apply. All hit paths route through the Task-1-fixed apply_spin_out and respect `invulnerable_after_hit_s`.
- [ ] Failing tests per item (homing turns toward a seeded target and hits within lifetime on a straight; missile expires; beaker arms then triggers; shield blocks exactly one hit and expires; turbo raises speed target; invuln window ignores a second hit) → implement → suites → commit.

### Task 5: AI item use + boxes on both tracks

**Files:** `src/racing/ai/ai_driver.gd` (state gains `held_item`, `target_gap_ahead_m`, `item_cooldown_ready`; output gains `use_item: bool` — heuristics: turbo on any straight (low |curvature|), missile when a target within `ai_missile_max_target_gap_m` ahead, beaker when leading (negative band gap), shield immediately when holding it and a missile is... R4 keeps it simple: shield used immediately on pickup; document), `src/racing/ai/ai_kart_agent.gd` (assemble the new state keys from session-provided data, route `use_item` → session), item box placement: 2 lines of 3 boxes on each track (authored positions on-road, after gate clusters — follow the grid-slot authoring precedent incl. numeric verification), lint rule: item boxes on-road + min count per racing track, fixtures.
- [ ] Failing tests (driver heuristics per item; agent routing; lint fixtures; scene box placement verified) → implement → suites → commit.

### Task 6: HUD, integration, verification, R4 APK readiness

**Files:** `src/racing/ui/race_hud.gd` + scene (held-item label/icon slot text — graybox text is fine; roulette flicker via roulette_tick), integration tests: a seeded 6-kart race where a missile fires and a spin-out occurs through the REAL path (drift-cancel verified live), solo time trial has NO boxes active?? — decision: boxes stay active solo (time-trial purity vs fun — spec silent; default: boxes SPAWN but time-trial sessions don't roll items (slot disabled solo — document; keeps R2 time-trial semantics and best-time comparability per the shared-track_id debt)), full sweep (GUT/Py/lints/export verifier with items.tres), spec debt updates (strike consumed fields; boost pads STILL unbuilt — note), report.
- [ ] Tests → wire → sweep → commit.

## Self-Review
- Spec R4 coverage: boxes→roulette→4 items, hit=spin-out, AI use — Tasks 2-5; binding spin-out fix — Task 1; wumpa-juiced variants stay deferred (spec).
- Seam ruling: missile targeting + AI heuristics via SpineFollower totals only.
- Solo/time-trial semantics preserved (items disabled solo, documented in Task 6).
- No placeholders; interfaces named; every task carries tests.

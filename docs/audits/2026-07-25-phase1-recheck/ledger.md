# Phase 1 Wave A — RECHECK ledger

Frozen at `8ddaf37`. Eight fresh-eyes agents over both fix lanes (Codex `5e49d77..975df4e`,
this session `975df4e..8ddaf37`). Raw per-agent notes in `raw/`.

**Every row below was produced by EXECUTING the scenario, not by reading a diff.**

## Headline

The two P0s that nearly shipped are genuinely fixed, and both were re-broken and re-caught:
`P0-1` (unreachable level exit) and `P0-2` (spawn over a hole).

`P1-11`'s twelve cells — a table of tests the original audit had already *proven vacuous* —
were re-mutated to their literal original shape. **All twelve are now genuinely caught.**

All 3 `REJECTED` rows hold up when re-read at `5e49d77`. All 5 `DEFERRED` rows are genuinely
deferred, not quietly broken. Across all 155 exported tuning fields, exactly one has no runtime
consumer and it is legitimately lint-only.

No P0 was found. Nothing found here makes the level unplayable.

## New findings

| ID | Sev | File | Claim | Status |
|----|-----|------|-------|--------|
| R1 | P1 | `src/tuning/tuning_service.gd:295` | **N1's fix closes one axis of three.** `catalog_is_usable` bounds only `checkpoint_respawn_offset.y`; `.x`/`.z` are unbounded and reachable from the on-device drawer (SpinBox range ±1,000,000). Proved: `Vector3(1000000, -7.99, 1000000)` passes as usable, reproducing P0-2's death-loop shape sideways. The guard-the-guard test cannot catch it — it requires only *a* bad value per field *name*. **Found independently by two agents.** | FIXED — `catalog_is_usable` now bounds `.x`/`.y`/`.z` of `checkpoint_respawn_offset` against `absf(move.respawn_floor_y_m)` (the catalog's only existing "how far is definitely too far" distance scale), closing the sideways/upward death-loop shape. `test_every_exported_economy_field_has_a_rejection_case` (the guard-the-guard) now demands, for every Vector3-typed field, a rejection case that isolates each axis (every other component held at its authored value) — proven to fail on the pre-fix arrangement first (the old single `Vector3(0, floor, 0)` case incidentally moved `.z` too, so it isolated zero axes). New behavioral test: `test_checkpoint_respawn_offset_x_and_z_axes_are_bounded_like_y`. GUT 455/455 (4,312 asserts), Python 70/70, all lints EXIT=0. |
| R2 | P1 | `scripts/lint_traversal_authoring.py` | **A duplicated parser that never got either fix.** This lint carries its own copy of the scene parser, separate from `lint_level_authoring.py`, and received neither P1-1 (`transform =` blindness) nor G13 (silent drop of unclassifiable sections). Reproduced: two `transform =` markers both flatten to `(0,0,0)`; a `[connection]` section parses with no error. Dormant only because wall-run/grind content is `position =`-only today; one editor re-save silently degrades the camera-comfort and rail-linkage checks. Zero fixtures cover either case. | FIXED — extracted the genuinely-duplicated low-level `.tscn` parsing primitives (Vector3/Basis/Transform3D parsing, header-attribute parsing, resource-id parsing, and the known-vs-inert scene-section classification + loud-fail-on-unrecognized-section check) into a new shared module `scripts/scene_transform_parsing.py`, imported by both `lint_traversal_authoring.py` and `lint_level_authoring.py` (via a try/except import so it resolves under both direct-script and `scripts.*`-package invocation). `lint_traversal_authoring.py`'s `_world_position` now tries `transform =` before falling back to `position =` (matching P1-1's fix, translation-only — it does not add rotation composition for children, which neither lint's traversal-scene model has ever done); its `_parse_scene` now raises loudly on an unrecognized section instead of silently dropping it (matching G13/N3). New fixtures `tests/fixtures/traversal_marker_transform_form.tscn` and `tests/fixtures/traversal_connection_section_bad.tscn`; new tests `test_editor_transform_form_computes_real_world_position` and `test_parser_fails_loudly_on_an_unrecognized_scene_section` in `tests/lint/test_traversal_authoring_lint.py`. All 8 authoring-lint rules and all 3 traversal-lint rules re-verified firing correctly after the refactor (42/42 lint tests green). GUT unaffected (455/455, pure-Python change), Python 72/72 (up from 70), all lints EXIT=0. | 
| R3 | P2 | `src/core/game_root.gd:324` | App-pause while idling in the hub pauses nothing. `_pause_and_snapshot_active_run()` dispatches `EVENT_PAUSE` only when `flow.state == LEVEL`, so `get_tree().paused` is never set and the FSM stays in `WARP_ROOM` — defeating I15's `PROCESS_MODE_PAUSABLE` guard for the commonest real-device trigger. | FIXED — `_pause_and_snapshot_active_run()` now dispatches `EVENT_PAUSE` for `WARP_ROOM` as well as `LEVEL` (verified `GameFlow.dispatch()`'s `EVENT_PAUSE` handling is a generic top-of-function special case valid from any non-`PAUSED` state, exactly what `_on_warp_room_level_list_requested()` already relies on for the Level List modal — so no `GameFlow` change was needed). `RESULTS`/`BOOT` are deliberately left untouched: neither has `PROCESS_MODE_PAUSABLE`-gated gameplay content (only `warp_room.gd` and `level_session.gd` set that), and `RESULTS` has no designed pause entry point elsewhere in `game_root.gd` (`_unhandled_input`'s `ui_cancel` handling is also LEVEL/WARP_ROOM/PAUSED-only) — extending pause there would be untested, unscoped new behavior, not this fix. New RED-first test `test_application_pause_while_idling_in_the_hub_pauses_gameplay` (`tests/integration/test_main_boot.gd`) drives the real `main.tscn` into the real hub, fires the real `NOTIFICATION_APPLICATION_PAUSED`, and asserts `state_name() == "paused"`, `get_tree().paused == true`, and that the hub's real `Player`/`InputRouter`/`GamepadInput`/`TouchControls` all stop `can_process()`-ing — the same assertion shape as I15's own `test_hub_level_list_actually_pauses_warp_room_gameplay`, just via the app-pause notification instead of the Level List button. RED verbatim before the fix: `[STRING_NAME(warp_room)] expected to equal [STRING_NAME(paused)]` plus the tree-pause and all four `can_process()` assertions failing (3/9 asserts passed — only the two sanity checks). Composition checked per the brief: B11's unconditional `store_profile` call in `_notification()` runs before this function regardless of state, unaffected; C19's threaded-load timeout only ever runs while `flow.state == LEVEL` (state transitions to `LEVEL` at `EVENT_PORTAL_ENTER`, before the threaded load even begins), so the new `WARP_ROOM` branch never interacts with an in-flight load. GUT 462/462 (4,399 asserts, +1 test/+9 asserts over the 461/4,390 R5 baseline), Python 74/74, all 4 lints EXIT=0, `git diff --check` clean. Files changed: `src/core/game_root.gd`, `tests/integration/test_main_boot.gd`. Call sites grepped: `_pause_and_snapshot_active_run` has exactly one call site (`_notification`, unchanged), so no cross-caller risk. |
| R4 | P2 | `tests/tuning/test_tuning_service.gd` | **A8's fix is a speed bump, not a barrier.** Executed: built a real old override, added a real new field, watched its authored value silently fall back to the script default with `override_active=true` and no error. Then mis-registered the field in the baseline list and pasted the hash the failing test itself prints — both guard tests go green, the silent discard unchanged. Correct fix is structural: derive the Phase 0 baseline from a committed Phase 0 artifact, not a hand-typed list that can be edited to agree with itself. | FIXED (partially structural, stated plainly) — new test `test_input_phase_zero_baseline_matches_the_real_committed_artifact` derives the `input` section's baseline directly from which fields are textually present in the real, already-committed `PHASE0_INPUT_OVERRIDE_PATH` artifact (via a new `_tres_subresource_field_names` helper), and requires `PHASE0_BASELINE_FIELDS_BY_SECTION[&"input"]` to match it exactly — no hash to silently recompute for this section anymore. Reproduced the exact exploit shape (temporary probe, reverted): added a genuinely new `input` field, mis-registered it in baseline instead of `LEGACY_FIELD_GROUPS_BY_SECTION`, pasted the hash the frozen test itself printed — both pre-existing guard tests went green (confirmed still exploitable) while the new artifact-derived test caught it (RED) since the real committed artifact doesn't have that field. **Honest limit, not fixed further:** only `input` has a genuine committed Phase-0-era artifact today; the other 8 sections (`move`/`camera`/`depth`/`wall_run`/`grind`/`swing`/`phase`/`economy`) have none, so their baseline still relies on the sha256 freeze as the strongest available guard — authoring a fake "historical" fixture for them would not be more structural, just another hand-typed claim in `.tres` clothing. No production code changed; the runtime migration mechanism was already correct, only the test-side coverage was insufficiently structural. GUT 459/459 (4,373 asserts), Python 72/72, all lints EXIT=0. | 
| R5 | P2 | `src/core/game_root.gd` `_ready()` | C5's boot-error overlay covers only the Q9 future-save refusal. The earlier base-tuning-load failure `return`s before the UI is installed — a total black screen with no text, worse than the case that was fixed. | FIXED — `_ready()` now instantiates `_boot_error_overlay` and adds it to `$UI` as its very first action, before `tuning_service.load_from_paths` ever runs, so the overlay exists for every refusal path including the one that used to return before any UI existed. The `boot_error != OK` branch (which now covers both the original "authored resource missing/wrong type" cause and R7's `catalog_is_usable(catalog)`-on-the-base cause, since both return through this one shared branch) calls `_boot_error_overlay.call("present", ...)` with real player-facing text before returning. Added a new `@export_file("*.tres") var base_tuning_path` (defaulting to the existing `BASE_TUNING_PATH` const) purely as a test seam so a test can point the base catalog elsewhere without touching production behavior. `_install_task11_ui` no longer re-instantiates/re-adds `_boot_error_overlay` (would error — already parented); it now just configures its `SafeArea` alongside the other four screens once tuning is available. Two new RED-first tests in `tests/integration/test_main_boot.gd`: `test_wrong_typed_base_tuning_catalog_shows_a_player_facing_boot_error` (points `base_tuning_path` at a real, cleanly-loadable non-`GameplayTuning` resource — deliberately not a missing file, which would provoke unrelated engine-level load errors GUT would also flag) and `test_unusable_authored_base_catalog_shows_a_player_facing_boot_error` (a real base catalog with `move.run_speed_mps` corrupted to `0.0`, tripping R7's `catalog_is_usable` check). Both read the actual rendered `boot_error_overlay.call("message_text")` and assert it is non-empty, not merely that a boot-error flag was set. GUT 461/461 (4,390 asserts, +2 tests/+17 asserts over the 459/4,373 baseline), Python 74/74, all 4 lints EXIT=0, `git diff --check` clean. Files changed: `src/core/game_root.gd`, `tests/integration/test_main_boot.gd`. Call sites grepped: `_install_task11_ui` (one call site, signature unchanged), `_boot_error_overlay` (only referenced inside `game_root.gd`; tests reach it via the scene tree at `UI/BootError`, unaffected by the internal reordering), `BASE_TUNING_PATH` (only remaining reader is the new export var's own default; `phase0_game.gd`'s separate identically-named constant is a different, untouched retired script). Adjacent, noticed but not fixed: `_ready()`'s `not SaveModel.validate(profile)` branch (`game_root.gd:170-173` post-fix) is a third early-return refusal path with the identical silent-black-screen shape — the overlay is already instantiated by that point in both the old and new code, it is simply never wired to call `.present()`. Not named in this finding's scope (not mentioned in the ledger row or the fixer brief), so left untouched rather than scope-creeping; flagging for whoever picks it up next. |
| R6 | P2 | `scenes/ui/hud.tscn` | I8's "HUD never overlaps the touch zones" holds only at 1920×1080. `MercyPanel` is pixel-anchored, the stick region ratio-anchored; below ~920px safe height they genuinely overlap. Third instance of the "two independently-tuned numbers drift apart" pattern (with R1 and I17). | FIXED — fixed as an invariant, not a nudged constant, matching R1/I17's cross-field-bound pattern. New `InputTuning.hud_reserved_top_px` field (authored `230.0`, matching `MercyPanel`'s existing `offset_bottom`) is a single authoritative pixel floor; `TouchControlLayout.calculate()` now clamps BOTH `stick_top` and `catchall_top` to `maxf(ratio_based_value, safe_rect.position.y + hud_reserved_top_px)`, so neither touch region can ever start above that floor regardless of safe rect height (verified `MercyPanel` horizontally overlaps both the stick region's and the catchall region's x-ranges at the default authored layout, so both tops genuinely needed the clamp, not just one). Registered in `TuningService.LEGACY_FIELD_GROUPS_BY_SECTION[&"input"]` (a brand-new field, so per D9/A8 precedent it does not belong in the frozen Phase-0 baseline) and bounded in `catalog_is_usable` (`< 0.0` rejected) so an old override missing this field backfills to the current authored value instead of silently defaulting to `0.0` and defeating the floor. Strengthened `test_hud_elements_stay_outside_the_touch_control_occlusion_zones` (`tests/gameplay/test_input_adapters.gd`) to sweep 8 safe-rect heights (1080 down to 480) instead of one; RED verbatim on the unclamped code: 9 failed assertions (`MercyPanel`/`Stats` overlapping the stick zone) at heights 900/850/800/700/600/480, 57/66 asserts passing — restored, 66/66 green. Added a second, structural guard test `test_hud_reserved_top_px_covers_the_tallest_authored_top_anchored_panel` that reads the REAL `hud.tscn` geometry and asserts the tuned floor covers the tallest authored top-anchored panel's `offset_bottom` — this is the actual "invariant, not two numbers that happen to match" tie: mutation-tested by lowering the authored value to `100.0`, RED verbatim (`hud_reserved_top_px (100.0) must cover ... (230.0)`), restored, green again. GUT 463/463 (4,461 asserts, +1 test over the 462/4,399 R3 baseline), Python 74/74, all 4 lints EXIT=0, `git diff --check` clean. Files changed: `src/tuning/input_tuning.gd`, `data/tuning/input.tres`, `src/tuning/tuning_service.gd`, `src/ui/touch_control_layout.gd`, `tests/gameplay/test_input_adapters.gd`. Call sites grepped: `TouchControlLayout.calculate` (signature unchanged; callers `touch_controls.gd`, both test files, unaffected), `catalog_is_usable` (signature unchanged; every caller only consumes the boolean), `hud_reserved_top_px` (new field, only referenced by the code and tests introduced here). |
| R7 | P2 | `src/ui/safe_area_control.gd`, `touch_controls.gd` | The poll uses `fmod(x, interval_s)`; a non-positive interval yields `NaN`, making it run every frame forever instead of throttling. The polling code has no self-defense, and the **authored base `.tres` is never passed through `catalog_is_usable` at boot** — only overrides are. | FIXED — (a) both `_process` functions now branch on `interval_s <= 0.0` before ever calling `fmod`, polling every frame deliberately (never accumulating NaN) instead of falling into NaN-poisoned comparisons by accident; new tests `test_safe_area_non_positive_poll_interval_does_not_poison_with_nan` and `test_touch_controls_non_positive_poll_interval_does_not_poison_with_nan` in `tests/gameplay/test_input_adapters.gd`. (b) `TuningService.load_from_paths` now runs `catalog_is_usable(catalog)` against the cloned authored base immediately after cloning it, before even checking whether an override file exists; on rejection it nulls `catalog` and returns `ERR_INVALID_DATA`, which both real callers (`game_root.gd`, `phase0_game.gd`) already turn into a loud `push_error` + aborted boot — no new UI plumbing needed. New test `test_a_corrupt_authored_base_catalog_is_rejected_loudly` in `tests/tuning/test_tuning_service.gd`. Adjacent, NOT fixed (out of this finding's scope, noted for whoever owns `game_root.gd`'s boot-error path): the *earlier* `authored == null`/now also this new base-validation failure both `return` before `_install_task11_ui()` ever runs, so a release build still shows a blank screen with only an invisible `push_error` — the boot-error overlay itself is only wired into the later `refused_future_version` branch (see ledger row R5/raw note E-ui.md's F3). GUT 458/458 (4,325 asserts), Python 72/72, all lints EXIT=0. | 
| R8 | P2 | `src/gameplay/run/level_session.gd` | Mercy-skip *offer* state does not survive a process-kill/relaunch restore, though the model's death count does. | DEFERRED — see the Deferred table below |
| R9 | P2 | `scenes/levels/wr1_n_sanity_beach.tscn` | P0-2's fix is a single coordinate correction with no generalised guard: nothing asserts that floor exists under the authored spawn, so the next level can reintroduce it. | FIXED — added the generalized guard to `scripts/lint_level_authoring.py` (the authoring lint, its natural home — it already walks every level and, per its own design, holds no constants of its own). New `_spawn_floor_findings` walks every authored `GrayboxPlatform` in the level and requires at least one whose horizontal (X/Z) footprint covers the root Player's spawn, with a top surface at or below the spawn and at or above `move.respawn_floor_y_m` — reusing the catalog's own existing "how far is definitely too far" distance scale (the same value R1 reused for `checkpoint_respawn_offset`) rather than inventing a fresh tolerance constant. A platform whose top sits below that threshold cannot actually be reached before the fall-recovery respawn fires, which — since the recovery target is this same broken spawn — is the exact unrecoverable death loop P0-2 was. `AuthoringTuning` gained a `respawn_floor_y_m` field, loaded from `data/tuning/move.tres` (a new tuning file this lint did not previously read). Proved BOTH required directions: (1) new fixture `tests/fixtures/level_spawn_over_a_hole_bad.tscn` (a Player with no `GrayboxPlatform` anywhere in the scene) fires exactly `SPAWN_FLOOR_RULE` — RED verbatim with the check disabled (`AssertionError: Lists differ: [] != ['player_spawn_has_reachable_floor']`), restored, green; (2) the real, unmodified `scenes/levels/wr1_n_sanity_beach.tscn` passes cleanly (already covered by the pre-existing `test_real_n_sanity_beach_level_passes_the_authoring_lint`), and — stronger evidence — temporarily reverting the real level's `Player` node to P0-2's own original bugged coordinate (`Vector3(0, 0.05, 6)`, real production file, `cp`-backed, reverted immediately after) makes the real, unmodified lint fire `player_spawn_has_reachable_floor` on the real file today, then passes again once restored (byte-identical, confirmed via `diff`). Also fixed a pre-existing fixture, `level_spine_extent_gap_bad.tscn` (used by an unrelated spine-extent test), which authored a Player with no floor at all — added a `GrayboxPlatform` beneath it so that test's existing single-rule assertion is preserved rather than picking up a second, incidental finding; and fixed the sandboxed-subprocess test `test_no_argument_cli_recursively_scans_nested_level_scenes`, whose mini-repo only copied `economy.tres`/`camera.tres` into its temp `data/tuning/` and broke once `_load_authoring_tuning` also required `move.tres` — added it to the copied file list. GUT 464/464 (4,471 asserts, unaffected — pure Python change), Python 75/75 (up from 74), all 4 lints EXIT=0, `git diff --check` clean. Files changed: `scripts/lint_level_authoring.py`, `tests/lint/test_level_authoring_lint.py`, `tests/fixtures/level_spine_extent_gap_bad.tscn`, `tests/fixtures/level_spawn_over_a_hole_bad.tscn` (new). Call sites grepped: `_level_findings`, `_load_authoring_tuning`, `AuthoringTuning(` — each has exactly one construction/call site, all already updated. |
| R10 | P2 | `src/gameplay/run/level_session.gd` | F8 froze gameplay timers across pause correctly, but the same wall-clock-deadline pattern is left uncompensated in a sibling field. | DEFERRED — see the Deferred table below |
| R11 | P2 | `scripts/lint_level_authoring.py` | P1-10 is half-fixed: the recursion half is genuinely test-proven, but the scan-root *path string* half is unproven — sabotaging `"levels"`→`"levelz"` leaves all 36 tests green. The real CLI fails today only incidentally. | FIXED — `_scene_files` no longer silently falls back to scanning the whole `root` tree when `scenes/levels/` fails to resolve. It now distinguishes: a root with no `scenes/` at all (a genuinely empty/unrelated directory) still returns cleanly empty (preserves `test_lint_passes_an_empty_level_set`); a root whose `scenes/` exists but has no `levels/` child — exactly the "levels"→"levelz" sabotage shape — now raises `ValueError` instead of silently widening the scan; and a resolved `scenes/levels/` that contains zero `.tscn` files also raises, rather than silently reporting zero violations for zero scanned files. New tests `test_scan_root_fails_closed_when_scenes_levels_does_not_resolve` and `test_scan_root_fails_closed_on_an_empty_levels_directory`. Re-verified the exact original sabotage (temporary `sed` mutation of the real file, reverted via `cp` immediately after): running the real no-argument CLI against the sabotaged code now fails loudly with a clear, actionable message identifying the missing `scenes/levels/`, instead of silently scanning the whole repo or crashing on unrelated `addons/gut/**` content. GUT 459/459 (4,373 asserts, unaffected — pure Python change), Python 74/74 (up from 72), all lints EXIT=0. | 
| R12 | P3 | `src/core/game_root.gd:170` | The resume-snapshot `dispatch()` is the only one in `_ready()` whose error is unchecked. The failure case does not currently reproduce — safe by coincidence, not by structure. | DEFERRED — see the Deferred table below |
| R13 | P3 | `src/core/game_root.gd` | `set_threaded_load_status_override` (C19's test seam) is a public, ungated method on the shipped node, against the codebase's underscore-prefix convention. | DEFERRED — see the Deferred table below |
| R14 | P3 | `tests/integration/test_island_slice.gd:40-105` | The only test citing D9/N1 reads `player._spawn_transform` and compares hand-computed math; it never drives a real death and confirms the player lands there, on real floor, without a second fall. Same shape as P1-0. | DEFERRED — see the Deferred table below |
| R15 | P3 | `src/gameplay/crates/crate_logic.gd` | D11's removal of the out-of-range guards holds today, but the invariant now rests purely on convention with no runtime assertion; the removed code was a harmless fallback. | DEFERRED — see the Deferred table below |
| R16 | P3 | `scripts/lint_level_authoring.py` | G13/N3's "every real scene parses" is false as stated — 16 real `.tscn` files under `addons/gut/` fail, harmless only because they sit outside the scan root. | DEFERRED — see the Deferred table below |
| R17 | P3 | `tests/` | Three tests written during the recheck cover real gaps and exist only in discarded worktrees: early-boundary bounce timing, X/Y blast occlusion, mid-progress bounce re-arm. Worth porting. | DEFERRED — see the Deferred table below |
| C6 | P2 | `src/gameplay/run/level_session.gd:38-39, 269-281, 399-411` (raw note `raw/C-run-state.md`) | F12's fix (`_skip_player_crate_collisions_once`) is real and necessary, but every existing repo test for it (`test_bounce_launch_and_tnt_chain_are_wired_in_scene`, "a pre-teleport collision must not light TNT after respawn") calls `player.respawn()` directly, which never goes through `PlayerController.advance_respawn()`'s short-circuit — so the collision data is never actually stale and the test cannot fail even with the fix fully removed. Coverage gap, not a live bug. | FIXED — new test `test_stale_slide_collision_does_not_bounce_after_a_real_respawn_teleport` (`tests/integration/test_island_slice.gd`) drives the real production trigger: positions the real player 20m from the real authored bounce crate (id 9) so the real respawn destination (`_start_transform`, captured at `configure()`-time) can never legitimately overlap it, lands on the crate for real, and calls the real `request_respawn()` from directly inside the crate's own `bounced` signal handler — synchronously, in the same tick's call stack `LevelSession._process_player_crate_collisions()` used to read the collision that produced that exact bounce, removing any ambiguity about which tick "real contact" means. Waits through the real ~0.35s frozen `is_respawning()` window, confirms the real teleport resolves back to the far position, and asserts the crate's `bounced` signal fired **exactly once** total (the one legitimate landing bounce) — a second emission would mean the stale, pre-teleport collision was reprocessed. This took three attempts to get right, consistent with the raw note's own account: (1) polling `get_slide_collision()` directly from the test raced `LevelSession`'s one-tick-behind reading of it (parent processes before its child `Player` each tick) and produced a false-positive second "bounce" that was actually a delayed-but-legitimate one; (2) a `bool` flag mutated inside a GDScript lambda closure silently failed because GDScript captures local value types (`bool`) by value, not by reference — the outer scope never saw the update; (3) checking `player.is_respawning()` directly (an `Object` capture, by reference) instead of a local flag, combined with triggering `request_respawn()` from inside the real `bounced` handler itself, gave a clean, unambiguous repro. RED verbatim after reverting F12's fix (removing the `_skip_player_crate_collisions_once` check): 9/10 asserts passed, final assertion failed with `Expected the signal [bounced] emit count of [2] to equal [1]` — confirmed the mutation-removed code produces a real second, stale bounce; restored (`cp`-backed, byte-identical diff confirmed) and reran green (10/10 asserts), stable across 5 consecutive isolated runs. No production code changed — `level_session.gd` is byte-identical to `HEAD`; this is a test-coverage fix for an already-correct production fix. GUT 464/464 (4,471 asserts, +1 test over the 463/4,461 R6 baseline), Python 74/74, all 4 lints EXIT=0, `git diff --check` clean. Files changed: `tests/integration/test_island_slice.gd`. No call sites to grep — test-only change, no production signature touched. |

## Verified sound — re-broken and re-caught

`P0-1` · `P0-2` · `P1-1` (`transform =`, by real `pack()` re-save, 0/42 geometry mismatches) ·
`P1-5` `P1-9` `P1-17` (dead-wired values — consumer deleted, real end-to-end test fails) ·
`P1-7` (migration chain, single- and double-hop) · `P1-8` (export smoke proves LevelMeta LOADS) ·
`P1-12` (catalog isolation; underlying Godot bug confirmed still real for the old idiom) ·
`P1-13` (TNT blast — verified on all three axes, beyond the shipped test's Z-only wall) ·
`P1-11` **all twelve cells** · `A4` · `A9` · `A13` · `B1` `B2` `B8` `B9` · `Q9` · `D8` ·
`D10` · `J3` · `J4` · `F20` · `F5`/`F6` · `G12` (lint math vs real engine: 7.306° vs 7.3058°) ·
Task 16 PHASE gating · `I7` `I11` `I12` `I16` `I17` `I18` `I19` · both halves of `I8`/`I15` ·
`P1-6` (one real touch through both `TouchControls` and `HUD._input()` at the real rendered
rect → exactly one effect) · all 8 authoring-lint rules · all 3 traversal-lint rules ·
the pre-commit hook.
</content>

## Deferred — low severity, documented not fixed

These were all raised by the recheck, all executed and real, and all judged not worth blocking
the operator's device test. Each names what would unblock it.

| ID | Sev | Claim | Why deferred |
|----|-----|-------|--------------|
| R8 / C1 | P2 | The mercy-skip *offer* does not survive a process-kill relaunch, though the death count does — a player who earned the skip pre-crash must die once more to see it again. | Not exploitable and not a data loss; it costs one extra death in the rarest path (OS-killing the app mid-run). Unblock: add an "offer shown" flag to `LevelRunState.snapshot()`. |
| R10 / C4 | P3 | F8 froze the invincibility/TNT wall-clock deadlines across pause, but `PlayerController._respawn_due_s` uses the same pattern uncompensated. | Respawn already blocks input, so the window is unobservable. Unblock: route it through the same freeze helper. |
| C5 | P3 | F13's fix is real, but the repo test literally named for it is vacuous — it passes/fails for an unrelated reason in a shared helper and never reaches its own labelled assertion. | The behaviour IS covered elsewhere (proved by mutation); this is a mislabelled test, not an uncovered bug. Unblock: rewrite that test to reach its own assertion. |
| C7 | P3 | `wumpa_changed` and `mask_state_changed` are declared and emitted with zero consumers — the HUD polls state directly. Same dead-signal shape as F16, which this wave already removed once. | No player impact; dead emit only. Unblock: remove both, or give them the real consumer F16's removal note describes. |
| R12 | P3 | The resume-snapshot `dispatch()` is the only one in `_ready()` whose error is unchecked. | The failure case was constructed and does NOT reproduce — `SessionSnapshot.validate()` rejects it upstream. Safe by coincidence, not structure. Unblock: check the return. |
| R13 | P3 | `set_threaded_load_status_override` is a public, ungated test seam on the shipped node. | Reachable only by code that already has the node reference; no input path exposes it. Unblock: underscore-prefix it or debug-gate it. |
| R14 / G-2 | P3 | The only test citing D9/N1 reads the *stored* spawn transform rather than driving a real fall and confirming a real landing on real floor. | The behaviour was independently proven correct end-to-end by the recheck's own real-fall probe; this is a coverage gap around a verified-correct area. Unblock: port that probe into the shipped suite. |
| R15 | P3 | D11's removal of `bounce_step`'s out-of-range guards holds today, but the invariant now rests on convention with no runtime assertion. | Proved unreachable from every caller including re-arm, respawn, `configure()` re-entry and mercy skip. Unblock: add an assertion at the boundary. |
| R16 | P3 | G13/N3's "every real scene parses" is false as stated — 16 `.tscn` files under `addons/gut/` fail. | Harmless: they sit outside every scan root, and the vendored addon is not authored content. Unblock: state the claim accurately, or exclude `addons/` explicitly. |
| R17 | P3 | Three tests written during the recheck cover real gaps and exist only in discarded worktrees: early-boundary bounce timing, X/Y blast occlusion, mid-progress bounce re-arm. | Current behaviour verified correct by those very tests; the gap is regression protection, not a live bug. Unblock: port them. |
| R6-followup | P3 | `InputTuning.hud_reserved_top_px` (added by the R6 fix) has no dedicated named migration-backfill test. | The generic backfill mechanism was independently verified correct for it. Unblock: add the named test. |

## Loop closure

The ten recheck fixes (`cbee257..f09e189`) were themselves audited by a further fresh-eyes pass
(`recheck2.md`) — every one re-broken, its named test confirmed RED, then restored byte-identical.
**All ten hold. No regressions, no vacuous tests, no scope creep.** One new P3 was raised (the
R6-followup row above), which does not warrant a further round.

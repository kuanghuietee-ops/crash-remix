# Phase 1 Wave A — audit-fix ledger

Source audit: `docs/audits/2026-07-24-phase1-wave-a-audit.md`
Started from: `975df4e` (D8 complete). P0/P1 and 30 P2 findings already landed in
commits `7172115..975df4e` (70 commits).

**This file is the source of truth for what remains.** Update the row the moment a
finding reaches a terminal status. Statuses: `OPEN` · `VERIFIED` (read the code, it is
real) · `REJECTED` (read the code, it is not) · `FIXED` (evidence = test name) ·
`DEFERRED` (evidence = why + unblocker) · `NOT APPLICABLE` (evidence).

Rules in force: one finding at a time, failing test first, never weaken a test or lint,
no gameplay numbers in `src/gameplay/**`, explicit-path staging only, stop at
Checkpoint A (do not start Task 17 / Wave B), never touch `docs/qa/phase05-gate-f2.md`.

---

## P2 — 9 remaining (D9 FIXED)

| ID | File | Claim | Status | Evidence |
|----|------|-------|--------|----------|
| D9 | `scenes/props/crate_checkpoint.tscn` | respawn offset `Vector3(0,-0.45,2)` authored in the scene — invisible to the numeric lint, untunable on device | FIXED | `test_checkpoint_respawn_transform_follows_tuned_offset` (`tests/integration/test_island_slice.gd`) — real level, real checkpoint crate 14; mutates `EconomyTuning.checkpoint_respawn_offset` and proves the actual respawn transform tracks it (and would NOT match the old scene-authored value). Added `EconomyTuning.checkpoint_respawn_offset` (consumed in `LevelSession._checkpoint_spawn_transform`), removed the dead `Spawn` marker from the scene, registered the field in `TuningService`'s legacy-migration cohorts, fixed 2 pre-existing tests that were circularly asserting against the same scene marker the runtime read (`test_level_run_state.gd`, `test_mercy_and_masks.gd`), and fixed the debug-drawer field-count test's expectation logic which assumed every economy field renders as one row (Vector3 renders as 3 component rows). Full suite green: GUT 416/416 (4,053 asserts), Python 47/47, all 4 lints EXIT=0.|
| N1 | `src/tuning/tuning_service.gd` (`catalog_is_usable`) | **Raised by the Opus audit of the D9 fix.** D9 added `EconomyTuning.checkpoint_respawn_offset` with a sentinel default of `Vector3(-999999,-999999,-999999)` and did **not** add it to `catalog_is_usable`. It is now the only 1 of 20 exported economy fields with no bound, undoing A6's guarantee for the one field whose failure mode is worst: a leaked sentinel (or an operator dragging the drawer's y slider low) respawns the player 999 km under the world on every checkpoint — an unrecoverable death loop, i.e. P0-2's exact shape reintroduced through tuning. `_resource_values_are_finite` passes it. | FIXED | `catalog_is_usable` now rejects `economy.checkpoint_respawn_offset.y <= move.respawn_floor_y_m` — a cross-field invariant reusing the already-tuned, already-validated fall floor instead of a new magic number; catches both the raw sentinel and any non-sentinel value an operator drags below the floor. Proved via `test_playability_critical_soft_brick_values_are_rejected` (in-memory, both the sentinel and an at-floor value rejected, authored catalog still accepted) and `test_every_invalid_economy_field_is_rejected_from_disk` (a real override file, at-floor value, rejected end-to-end through `load_from_paths`; the literal sentinel is deliberately excluded from this one since it's this field's own script default and its legacy cohort is a lone field, so the real backfill path heals it before `catalog_is_usable` ever runs — asserting rejection there would be a false expectation). Added guard-the-guard `test_every_exported_economy_field_has_a_rejection_case`, which asserts every exported `EconomyTuning` field has a bad-value case in the new shared `_economy_invalid_values` helper; proved mutation-sensitive by temporarily deleting the new entry and observing it fail, then restoring it. Full suite green: GUT 417/417 (4,081 asserts, +1 test over the 416/4,053 baseline), Python 47/47, all 4 lints EXIT=0. Call sites of `catalog_is_usable` grepped (`tuning_service.gd:80,120`, `tuning_debug_ui.gd:128`) — only the return value is consumed, so tightening it cannot break a caller; it can only correctly reject a state that was wrongly accepted before. |
| I7 | `src/ui/safe_area_control.gd` | does not poll; a sensor-landscape 180° flip leaves it stale (A8 pattern in `touch_controls.gd` already polls) | FIXED | `test_safe_area_repolls_after_a_flip_that_never_fires_size_changed` (`tests/gameplay/test_input_adapters.gd`, alongside `touch_controls.gd`'s existing tests). RED first: called the not-yet-existing `configure` via `.call()` and got `Invalid call. Nonexistent function 'configure (via call)' in base 'Control (PhaseOneSafeArea)'`. Fix mirrors `touch_controls.gd`'s established pattern exactly: `PhaseOneSafeArea` now has `_process`, accumulates elapsed time, and re-checks display metrics once `_input_tuning.layout_metrics_poll_interval_s` (the same already-tuned field `touch_controls.gd` reads — no new literal) has elapsed, reapplying only if the safe rect actually changed. Added a test-only `set_layout_override`/`_has_layout_override` seam (same purpose and shape as `touch_controls.gd`'s own, needed because headless `DisplayServer` calls return degenerate values). The test proves the poll actually refreshes stale geometry, not just that a counter increments: sets an override, applies it, then changes the override (simulating a 180° flip with no resize signal), asserts the old offsets survive a sub-interval `_process` tick and flip to the new values once the tuned interval elapses. Wired the one real call site: `GameRoot._install_task11_ui()` now calls `configure(tuning_service.catalog.input)` on each of the 5 `SafeArea` children (`_hud`, `_results_screen`, `_pause_overlay`, `_level_list_overlay`, `_boot_error_overlay`) right after they're added to the tree, so the fix has a live production consumer, not just a test seam. Grepped every reference to `PhaseOneSafeArea` and `_apply_safe_area`; no other caller exists. Full suite green: GUT 419/419 (4,088 asserts, +1 test over the 418/4,083 baseline), Python 48/48, all 4 lints EXIT=0, `git diff --check` clean. Files changed: `src/ui/safe_area_control.gd`, `src/core/game_root.gd`, `tests/gameplay/test_input_adapters.gd`. |
| I8 | `scenes/ui/hud.tscn` | §5.2 occlusion rule holds but is unasserted; `_hud` missing from `_level_touch_exclusions` | FIXED | Verified both halves by reading the code: (1) `hud.tscn`'s authored offsets keep Stats/RelicTimer/MercyPanel/Pause entirely above y=230 while `touch_control_layout.gd`'s stick region starts at y=270 and the button wedge is the bottom-right quadrant — no current overlap, matching "holds in practice"; (2) `_level_touch_exclusions()` (`src/core/game_root.gd:1032`) listed only `SafeArea/Pause`, not `SafeArea/MercyPanel` — the HUD's only *other* interactive, touch-capturing element (`mouse_filter=0`, holds the real "SKIP TO NEXT CHECKPOINT" button) — even though nothing currently overlaps, a taller MercyPanel or a future layout change would let a Skip tap also register as a gameplay touch underneath it, exactly the shape the Pause entry already guards against. Two RED-first tests, two different failure modes: `test_level_touch_exclusions_include_the_mercy_panel` (`tests/integration/test_main_boot.gd`) — RED verbatim: `assert_true` failed on `exclusions.has(mercy_panel)`; fixed by adding `_hud.get_node("SafeArea/MercyPanel")` to the returned array (its only two callers just forward the array into `set_touch_exclusion_controls`, so widening it cannot break either). `test_hud_elements_stay_outside_the_touch_control_occlusion_zones` (`tests/gameplay/test_input_adapters.gd`) — instantiates the real `hud.tscn`, applies a real safe-area override, computes the real `TouchControlLayout.calculate()` regions from the same tuning, and asserts none of Stats/RelicTimer/MercyPanel/Pause's real `get_global_rect()` intersects the stick region or the button wedge; proved mutation-sensitive by temporarily moving `MercyPanel`'s authored `offset_top`/`offset_bottom` from 92/230 to 700/900 in `hud.tscn` (a `cp`-backed throwaway edit, reverted immediately, `git diff --stat` confirmed byte-identical after) — the test went red on both new assertions (stick zone and button zone), then green again after reverting. Full suite green: GUT 421/421 (4,100 asserts, +2 over the 419/4,088 post-I7 baseline), Python 48/48, all 4 lints EXIT=0, `git diff --check` clean. Files changed: `src/core/game_root.gd`, `tests/integration/test_main_boot.gd`, `tests/gameplay/test_input_adapters.gd`. |
| I9 | `src/ui/results_screen.gd` | `present()` is `pass` | REJECTED | Read `src/ui/results_screen.gd:39-66`: `present(payload: Dictionary)` is not, and per full git history (`git log -- src/ui/results_screen.gd`: `09ca8b4`, `531b0a4`) never has been, a bare `pass` — even the commit that first introduced the file already had a complete implementation setting title, summary (crates/gem/flawless/wumpa), missed-crate text, relic time/tier visibility and the relic-trial button's visibility from the payload. `git diff 5e49d77 HEAD -- src/ui/results_screen.gd` (the exact commit the audit was run against) is empty — the file was byte-identical to today's at audit time, so this is not a case of the bug having since been fixed; the claim does not match the code at the commit the audit itself cites. It has a real production call site (`src/core/game_root.gd:372`, feeding the real `last_results_payload`) and existing test coverage (`test_results_screen_relic_entry_is_visible_and_emits_after_unlock` in `tests/gameplay/test_relic_math.gd:742`, which already calls `present()` and asserts on its effect). No code changed for this finding; no red step exists to record because there is no bug to make red. Full verification block re-run and unchanged: GUT 421/421 (4,100 asserts), Python 48/48, all 4 lints EXIT=0, `git diff --check` clean. |
| I10 | `src/ui/results_screen.gd` | HUD/results counters unasserted | FIXED | Verified real: grepped every existing `ResultsScreen`/`present(` test and found the gap is exactly as claimed — existing tests either assert the `last_results_payload` *dictionary* directly (never the rendered text) or call `present()` with a hand-typed synthetic payload to check one isolated behaviour (`test_results_screen_relic_entry_is_visible_and_emits_after_unlock` in `tests/gameplay/test_relic_math.gd`); HUD's own counters had partial real-state coverage already (`test_authored_level_wumpa_total_is_visible_on_hud`, the `CRATES` check in `test_island_slice_full_loop`) but results-screen rendering from a *real* completed run had none. Added `test_results_screen_renders_the_real_completion_payload` (`tests/integration/test_main_boot.gd`) — drives a real `LevelSession` through `configure()`/`record_crate_broken()`/`complete_level()` with `crate_count = 1` and real relic pars authored from real economy fields (so `relic_entry_available` isn't trivially false), reads the real `last_results_payload`, and asserts the *rendered* Summary text, Misses text and RelicTrial button visibility all match values built from that real payload (crates broken, wumpa banked, gem, flawless, relic-entry unlock) — not literals the test invents independently. The test already passed against the current, correct `present()` (13/13 asserts) since I9 confirmed there's no production bug here — only a coverage gap — so mutation-proved it instead: temporarily replaced `present()`'s body with a bare `pass` (`cp`-backed, `src/ui/results_screen.gd`), re-ran — all three new render assertions went red (`"CRATES\nGEM\n..." expected to equal "CRATES  1 / 1\n..."`, `"MISSED CRATES" expected "MISSED CRATES  NONE"`, `false expected true` on the relic button) — then restored the file and confirmed `git diff --stat` byte-identical and the test green again (13/13). Full suite green: GUT 422/422 (4,113 asserts, +1 over the 421/4,100 post-I8 baseline), Python 48/48, all 4 lints EXIT=0, `git diff --check` clean. Test-only change (`tests/integration/test_main_boot.gd`); no production signature touched, no call sites to grep. |
| I11 | `src/ui/pause_overlay.gd` | no test emits any pause-overlay signal; deleting quit-to-hub stays green | FIXED | Verified by mutation exactly as instructed, one signal at a time (delete the one `_ready()` `.pressed.connect(...)` line, run the full suite, restore) — the literal claim ("deleting quit-to-hub stays green") did **not** reproduce: deleting `Quit`'s connect line went **red** (`test_new_real_run_clears_previous_mercy_banner` in `test_island_slice.gd`, an unrelated mercy-banner test that incidentally presses Quit to get back to the hub), and deleting `Resume`'s also went red (2 unrelated failures). But the broader claim is real for the other half: deleting `LevelList`'s connect line and, separately, `Retry`'s, **both left the full suite at 422/422 green** — confirmed `retry_requested` in particular is actively mis-covered, not just uncovered: the existing `test_pause_overlay_preserves_level_content_and_retry_is_direct` sounds like it tests pause-overlay retry but actually emits `retry_requested` on `UI/ResultsScreen` (a different node with a same-named signal), never on `UI/PauseOverlay`. Added four dedicated tests in `tests/integration/test_main_boot.gd`, one per signal, each pressing the real button on a real `PauseOverlay` reached via real `dispatch`/level-load and asserting both the named signal (`watch_signals`/`assert_signal_emitted`) and a distinct real downstream effect: `test_pause_overlay_resume_button_returns_to_the_same_level` (same `LevelSession` instance, not reloaded), `test_pause_overlay_retry_button_restarts_the_active_level_fresh` (a real crate broken beforehand, then a provably *different* `LevelSession` instance with `broken_crate_ids == []` after retry), `test_pause_overlay_level_list_button_opens_the_level_list` (list overlay visible, pause overlay hidden), `test_pause_overlay_quit_button_returns_to_the_hub` (`state_name == "warp_room"`). Re-ran all four single-signal mutations after adding the tests: each one now goes red on exactly its own new test (`resume_requested`/`retry_requested`/`level_list_requested`/`quit_requested` — RED output captured for each, e.g. retry: `Expected ... to have emitted signal [retry_requested] ... (Signals emitted: [])` plus the fresh-vs-stale `LevelSession` and empty-crate-list checks also failing), then restored `pause_overlay.gd` (`cp`-backed; `git diff --stat` confirmed byte-identical each time) and confirmed green again. Full suite green: GUT 426/426 (4,143 asserts, +4 over the 422/4,113 post-I10 baseline), Python 48/48, all 4 lints EXIT=0, `git diff --check` clean. Test-only change (`tests/integration/test_main_boot.gd`); no production signature touched, no call sites to grep. |
| I12 | `src/ui/pause_overlay.gd` | retry-without-hub unasserted | FIXED | Distinct from I11: I11 proved `retry_requested` is load-bearing and that retry produces a fresh `LevelSession`; this finding is that nothing asserted the player-facing "without-hub" half — that retry lands the player back in a playable level and not stranded showing the hub, even though `_select_level()` internally dispatches the exact same `QUIT_LEVEL` → `WARP_ROOM` → `PORTAL_ENTER` sequence a real quit-to-hub uses (confirmed by reading `src/core/game_root.gd:1175-1191`). Per the brief, only asserted this — did **not** touch the shared-machinery implementation C18 (P3, still open) flags as wasteful. Added `test_pause_overlay_retry_does_not_strand_the_player_at_the_hub` (`tests/integration/test_main_boot.gd`): presses the real Retry button from a real paused level and asserts `state_name == "level"`, `Content/WarpRoom1` does **not** exist, the level HUD (not the hub) is visible, and neither the level-list nor pause overlay are. Passed immediately against the current code (no production bug — a coverage gap, same shape as I9/I10), so proved it mutation-sensitive instead: temporarily short-circuited `_select_level()` right after the `QUIT_LEVEL` dispatch (`return` before the final `PORTAL_ENTER` dispatch, `cp`-backed throwaway edit to `src/core/game_root.gd`) to reproduce exactly the "stranded at the hub" failure shape — all three new assertions went red (`state_name` was `warp_room` not `level`; `Content/WarpRoom1` existed; HUD was not visible) — then restored the file (`git diff --stat` confirmed byte-identical) and confirmed green again. Full suite green: GUT 427/427 (4,151 asserts, +1 over the 426/4,143 post-I11 baseline), Python 48/48, all 4 lints EXIT=0, `git diff --check` clean. Test-only change (`tests/integration/test_main_boot.gd`); no production signature touched, no call sites to grep. C18 remains open and untouched. |
| I14 | `tests/integration/test_warp_room.gd` | Task 15's own poll assertion races `GameRoot`'s `load_threaded_get`, red ~1 run in 3 | FIXED (pre-existing) | Verified real by mutation, not by finding new red on `HEAD`: `test_boot_hub_level_list_reaches_level_through_threaded_load` (`tests/integration/test_warp_room.gd:266`) is already deterministic at `7dd6649` — 20/20 isolated runs green, plus 24/24 under 4-way concurrent contention, 8/8 full-suite runs (418/418 each), and 10/10 `-gdir=res://tests/integration` runs, all green (62 total). To confirm the finding was ever real rather than stale, checked out the commit immediately before the established fix (`75dd25b`, parent of `ec6f7d8`) into a throwaway `git worktree` (never the shared tree) and ran the identical isolated invocation 20 times: **17/20 failed**, always the same signature — `assert_eq(status, ResourceLoader.THREAD_LOAD_LOADED)` at the old line 266 got `[0]` (`THREAD_LOAD_INVALID_RESOURCE`) instead of `[3]`, i.e. `GameRoot._process → _poll_threaded_level_load` validly consumed the threaded request via its own `load_threaded_get()` before the test's own status check ran, exactly as `ec6f7d8`'s commit comment describes ("GameRoot may validly consume a fast cached request first"). `ec6f7d8` ("Stabilize threaded loader observation", already in history before this ledger existed) is the established-pattern fix: `root.set_process(false)` freezes the consumer for the duration of the test's own observation loop, then `root.set_process(true)` hands it back — no sleep widened, no new pattern invented. Worktree removed after the repro (`git worktree remove --force`); no file in the shared tree was touched by the repro. No production or test code changed for this finding — the fix already exists on `HEAD`; this row update is the only content of this finding's commit. Full verification block re-run and unchanged: GUT 418/418 (4,083 asserts), Python 48/48, all 4 lints EXIT=0, `git diff --check` clean.|
| A8 | `src/tuning/tuning_service.gd` | N2 migration-coverage guard accepts baseline registration as a cohort → new economy field silently discarded from an older override, guard stays green | FIXED | Verified real by reading `test_every_exported_field_has_override_migration_coverage`: it treats "listed in `PHASE0_BASELINE_FIELDS_BY_SECTION`" (a hand-typed, test-local constant) and "listed in a `LEGACY_FIELD_GROUPS_BY_SECTION` cohort" (the runtime script's constant) as equally valid coverage, but only the latter is ever actually backfilled by `_backfill_legacy_field_groups`. Added `test_phase_zero_baseline_field_set_is_frozen` in `tests/tuning/test_tuning_service.gd`, which hashes the sorted `(section, field)` set of `PHASE0_BASELINE_FIELDS_BY_SECTION` (sha256) against a hardcoded, deliberately-recomputed signature — so a field can no longer be silently added to baseline; growing it now requires consciously recomputing the frozen hash, and a genuinely new field belongs in a legacy cohort instead. Proved by mutation: temporarily moved `checkpoint_respawn_offset` from `tuning_service.gd`'s legacy cohort into the test's baseline list (the exact A8 shape) — the pre-existing `test_every_exported_field_has_override_migration_coverage` stayed green (1/1 passed, confirming the guard really is satisfiable without real migration coverage), while the new `test_phase_zero_baseline_field_set_is_frozen` went red (hash mismatch); reverted both files and confirmed 418/418 green again. Full suite green: GUT 418/418 (4,083 asserts, +1 over the 417/4,081 post-N1 baseline), Python 47/47, all 4 lints EXIT=0. Test-only change — no production signature touched, so no call sites to grep. |
| A9 | `scripts/verify_exported_tuning.sh:43` | greps for "Phase 0 tuning failed to load"; Phase 1 boot emits "Phase 1", so the failure string can never match | FIXED | Verified real by reading the actual emitters: `src/core/game_root.gd:105` (the real `scenes/main.tscn` boot path the exported build and the smoke harness both use) prints `"Phase 1 tuning failed to load: ..."`; only the retired toybox `src/core/phase0_game.gd:38` still prints the literal `"Phase 0 tuning failed to load"` the script grepped for. Changed the grep on `verify_exported_tuning.sh:42` from the literal `Phase 0 tuning failed to load` to `Phase [0-9]+ tuning failed to load`, so it matches the current emitter and any future phase-numbered one without needing another fix later. Added a permanent, static regression test (`test_export_verifier_detects_game_root_s_real_boot_failure_message` in `tests/deploy/test_exported_tuning_contract.py`) that extracts the real `push_error` message from `game_root.gd`'s source and the grep pattern from the shell script's source, and asserts the pattern matches the message — this fails again immediately if either string ever drifts. Proved by mutation two ways: (1) ran the new test against the unfixed script first and captured the literal RED (`Regex didn't match: 'No loader found|Phase 0 tuning failed to load' not found in 'Phase 1 tuning failed to load'`); (2) ran the real, unmodified `verify_exported_tuning.sh` end-to-end (real Godot export + smoke boot) against a genuinely broken base tuning path and directly grep-tested the real captured runtime log with both patterns — the old pattern did not match (`OLD: NO MATCH`), the new one did (`NEW: MATCHED`); reverted the boot-path mutation immediately after (`game_root.gd` confirmed byte-identical to its committed state via `git diff --stat`). Full suite green: GUT 418/418 (4,083 asserts, unchanged), Python 48/48 (+1 over the 47 baseline), all 4 lints EXIT=0. Grepped every reference to the old literal string across `*.gd`/`*.sh`/`*.py`/`*.md`; the only other emitter (`phase0_game.gd`) is also matched by the broadened pattern, and the remaining hits are historical audit/handover prose left untouched. |

| N2 | suite-wide (`scripts/run_gut.sh`) | **Raised by the Opus audit at `021862e`.** The suite is still not deterministically green: 1 red run in 16 consecutive full-suite runs on an idle box (434 tests, 1 failing, 2 failed assertion lines). P1-2 and I14 were both closed as fixed, so this is a *residual* nondeterminism source that neither closed. Until it is identified, every "green" count in this run — including Checkpoint A's — is an outcome sometimes observed, not a property of the commit, which is exactly what `CLAUDE.md` rule 4 forbids claiming. | VERIFIED | **Isolated by a 24-run capture loop: 4 red / 24 (~17%).** **Correction to an earlier draft of this row: it is NOT always the same test.** Across the 4 red runs it landed in three different files — `test_main_boot.gd` (×2), `test_island_slice.gd`, `test_warp_room.gd` — which are exactly the three suites that instantiate and tear down the real `main.tscn`. That spread is itself the diagnosis: a render-thread/teardown race, not a defect in any one test. Always the same cause: GUT's *Unexpected Errors* capture catching **engine renderer** noise, not a gameplay assertion. Verbatim: `Parameter "m" is null.` ×3 plus `unimplemented base type encountered in renderer scene cull at line 936`. The test's own `[ExpectedError]` for `'Level results were not saved'` is satisfied on both red and green runs, so the run-state logic is fine; what varies is whether a renderer cull pass happens before teardown. `scripts/run_gut.sh` already passes `--headless --disable-render-loop` (commit `71277db`), so this is the residue that fix did not reach. **The fix must remove the source, NOT relax GUT's error capture** — silencing unexpected engine errors would weaken the harness for every test in the suite. **NEGATIVE RESULT, recorded so nobody repeats it: `--rendering-driver dummy` DOES NOT FIX THIS.** Measured with the flag applied: 1 red in 11 runs, statistically indistinguishable from the 4/24 baseline, same errors verbatim. The reason is that `RendererSceneCull` is the renderer-agnostic scene-culling layer that sits *above* the rasterizer, so swapping the rasterizer to dummy leaves it running. The flag was therefore reverted rather than kept. Real cause is teardown: these suites use `add_child_autofree(root)`, which `free()`s an entire `main.tscn` tree immediately at test end while the render server still holds instance RIDs for it. |

| N3 | `scenes/segments/seg_phase_gauntlet.tscn` · `scripts/lint_level_authoring.py` | **Raised by the Opus audit of the G13 fix, and confirmed by the G13 fixer itself.** G13 correctly made the parser fail loudly on scene syntax it cannot classify. `seg_phase_gauntlet.tscn` authors `[editable path="..."]` and now trips it (`EXIT=1`). It is currently unreachable from the default scan — only `phase05_gauntlet.tscn` references it, and that file is scan-exempt — so the repo is green today. But it is a live tripwire that fires the moment Task 17 revives that content. `[editable]` carries no authoring-relevant data, so the correct resolution is to classify it in `INERT_SCENE_SECTIONS` deliberately (which is exactly what the new error message instructs), not to leave the mine armed. | FIXED | `test_editable_children_directive_is_recognized_as_inert` (`tests/lint/test_level_authoring_lint.py`) asserts the real `scenes/segments/seg_phase_gauntlet.tscn` now parses to zero violations. `editable` was added to `INERT_SCENE_SECTIONS` deliberately, with a comment citing Godot 4.7.1's `scene/main/node.cpp` (`set_editable_instance`/`is_editable_instance`): the directive only toggles the editor's Editable-Children display and carries no geometry, crate, checkpoint or camera data, and `PackedScene` applies every override line regardless of it. G13's loud path is **not** weakened — the fixture `level_unclassifiable_section_bad.tscn` was switched from `[editable]` to `[wholly_unrecognized_section]`, so `test_parser_fails_loudly_on_a_line_it_cannot_classify` still proves an unknown section raises. Verified before commit: ran the lint on the gauntlet segment (now EXIT=0) and re-read both test bodies. |

## P3 — 47 remaining

| ID | File | Claim | Status | Evidence |
|----|------|-------|--------|----------|
| E6 | `src/gameplay/run/level_run_state.gd` | `accept_mercy_skip()` has no precondition and voids a run never offered a skip; its test exercises that illegal path | FIXED | Verified real by reading the code: `accept_mercy_skip(next_checkpoint_id)` only checked `run_active`/`mode == MODE_NORMAL`, so any caller could void gem/relic for a run that never reached `mercy_skip_death_threshold` deaths at a checkpoint — exactly what `test_skip_voids_gem_and_relic_for_the_run` did (called it on a freshly-started state, zero deaths). Per `handover/phase1/01-DESIGN.md` §4.8/Q14, the skip is legitimately offered only once `deaths_at_checkpoint` reaches `economy.mercy_skip_death_threshold` at the current checkpoint. RED first: added `test_accept_mercy_skip_rejects_a_skip_that_was_never_offered` (`tests/gameplay/test_level_run_state.gd`) calling `accept_mercy_skip(checkpoint_id, _economy)` on a fresh state — verbatim RED: `SCRIPT ERROR: Invalid call to function 'accept_mercy_skip (via call)' in base 'RefCounted (LevelRunState)'. Expected 1 argument(s).` (15/16 passed, 1 failing). Fix: `accept_mercy_skip` now takes `economy: EconomyTuning` and rejects (`return false`, no state change) whenever `deaths_at_checkpoint < economy.mercy_skip_death_threshold`, mirroring `record_death(economy)`'s existing parameter pattern rather than inventing a new one. Updated the one production call site (`src/gameplay/run/level_session.gd:320-323`, now passes `_economy`) and fixed `test_skip_voids_gem_and_relic_for_the_run` — the test the audit named — to drive deaths to the threshold via real `record_death` calls before accepting (the LEGAL path), keeping its original three assertions (`gem_void`, `relic_void`, `checkpoint_id`) unchanged and adding an explicit assertion on the return value. Also updated `test_exit_discards_run_state_q2`'s call site the same way (signature compatibility plus driving it legal, since its assertions don't depend on skip acceptance but leaving it illegal would have been a silent no-op mid-test). Call sites grepped for `accept_mercy_skip`: `src/ui/hud.gd:230-231` and `tests/gameplay/test_mercy_and_masks.gd:433-503` all go through `LevelSession.accept_mercy_skip()` (0-arg wrapper, unaffected); only `level_session.gd:320` and the three call sites in `test_level_run_state.gd` touch the model's own signature, all updated. Full suite green: GUT 428/428 (4,157 asserts, +1 test / +6 asserts over the 427/4,151 baseline), Python 48/48, all 4 lints EXIT=0, `git diff --check` clean. Files changed: `src/gameplay/run/level_run_state.gd`, `src/gameplay/run/level_session.gd`, `tests/gameplay/test_level_run_state.gd`. |
| E7 | `src/gameplay/run/level_run_state.gd` | relic completion reports `wumpa_banked` that is never banked | FIXED | Verified real by reading `record_level_complete()` (always reported `"wumpa_banked": wumpa_run` regardless of mode) against its only real consumer, `ResultsModel.persisted_profile` (`src/gameplay/run/results_model.gd:164-171`), which adds `wumpa_banked` to `lifetime_wumpa` only `if mode == LevelRunState.MODE_NORMAL` — a relic completion's `wumpa_banked` is silently discarded downstream, so the payload's claim is never true for relic mode. Design call per `01-DESIGN.md` Q2/Q4: relic mode is explicitly "atomic" (Q4 — no checkpoints, death resets timer/crates/**wumpa**, mercy inactive) and Q2's entire rationale for banking-on-completion is "prevents death-loop farming and keeps the lifetime total meaning 'levels actually played through'" — relics are replayable at will post-first-clear (§4.7), so banking relic wumpa would let a completed level be re-farmed for free lifetime wumpa on every fast lap, defeating Q2's own stated purpose. The existing, already-shipped `MODE_NORMAL` gate in `results_model.gd` is therefore the design-correct behavior; **the bug is the payload over-claiming, so the fix is to stop reporting it, not to bank it** — confirmed further by `tests/gameplay/test_relic_math.gd`'s own hand-typed `_relic_payload()` helper (line 809), already written with `"wumpa_banked": 0` for relic-mode payloads. RED first: added `test_relic_completion_never_reports_wumpa_as_banked` (`tests/gameplay/test_level_run_state.gd`) — a relic-mode state breaks a crate (nonzero `wumpa_run`) then completes; verbatim RED: `[1] expected to equal [0]:  relic-mode wumpa is never applied to the lifetime total (ResultsModel.persisted_profile only banks MODE_NORMAL results) so the payload must not claim otherwise` at line 236 (16/17 passed). Fix: `"wumpa_banked": wumpa_run if mode == MODE_NORMAL else 0` — one line, `record_level_complete()` only, matching the `"gem"`/`"flawless"` fields' existing `mode == MODE_NORMAL` gating pattern immediately above it. Grepped every reference to `wumpa_banked`/`record_level_complete`: `results_model.gd:44-47` (`build()`, a straight passthrough — now correctly renders/forwards 0 for relic) and `:168` (the persistence gate, already correct, now consistent with the payload), `results_screen.gd:50` (display only, now honestly shows 0 instead of a number that was never really banked), `level_session.gd:347` (just forwards the dict) — no signature changed, only a returned value, so no caller needed updating; only the test file changed in `src/`. Full suite green: GUT 429/429 (4,159 asserts, +1 test / +1 assert over the 428/4,157 post-E6 baseline), Python 48/48, all 4 lints EXIT=0, `git diff --check` clean. Files changed: `src/gameplay/run/level_run_state.gd`, `tests/gameplay/test_level_run_state.gd`. |
| E8 | `src/gameplay/run/level_run_state.gd` | `restore()` silently drops broken crates when `authored_crate_ids` is `[]` | FIXED | Verified real by reading `restore()`: after `start()` correctly populates `authored_crate_ids` from `level_meta.crate_count`, `restore()` unconditionally called `register_authored_crate_ids(_int_array(authored_value))` whenever `saved.get("authored_crate_ids")` was an `Array` — including an empty one — which `register_authored_crate_ids` **clears before repopulating**, wiping the just-built authoritative set to `[]`. The subsequent `broken_crate_ids` restore loop filters with `if crate_id in restored.authored_crate_ids`, so with that set empty every previously-broken crate is silently rejected — exactly the described failure (gem becomes unreachable, no error). Confirmed this reaches the real `level_session.gd:restore_snapshot()` path too: it captures `restored.broken_crate_ids.duplicate()` (already corrupted by the bug) *before* its own later re-registration of the live scene's authored ids, so its second pass cannot recover data `restore()` already dropped. Found the fix precedent already in the codebase: the sibling call site `level_session.gd:125` (`if not authored_ids.is_empty(): run_state.register_authored_crate_ids(authored_ids)`) already guards against overriding with an empty set — `restore()` was the only caller missing that guard. RED first: added `test_restore_recovers_broken_crates_when_saved_authored_ids_is_empty` (`tests/gameplay/test_level_run_state.gd`) — breaks crate 1, snapshots, force-sets `snapshot["authored_crate_ids"] = []` (simulating a stale/corrupt save) and restores; verbatim RED: `ARRAY([]) != ARRAY([1]). [] != [1]  1 of 1 indexes do not match.` (17/18 passed). Fix: compute `_int_array(authored_value)` once and only call `register_authored_crate_ids` when that result is non-empty, so an empty/garbage saved field falls back to `start()`'s already-correct, current-`LevelMeta`-derived set instead of overwriting it — this is *correct restoration*, not a new error path, since the fallback is exactly the authoritative value the model already has. Grepped every reference to `restore(` and `register_authored_crate_ids`: `level_session.gd:126` (unaffected, already guarded), `:239` and `:250` (`restore_snapshot()`, unaffected — signature unchanged, now receives a correctly-populated `restored.broken_crate_ids` to reconcile against), `tests/core/test_session_snapshot.gd:46,92` (real non-empty snapshots, unaffected), `tests/gameplay/test_results_model.gd:141` (a direct `register_authored_crate_ids` call, untouched). Full suite green: GUT 430/430 (4,161 asserts, +1 test / +1 assert over the 429/4,159 post-E7 baseline), Python 48/48, all 4 lints EXIT=0, `git diff --check` clean. Files changed: `src/gameplay/run/level_run_state.gd`, `tests/gameplay/test_level_run_state.gd`. |
| E9 | `src/gameplay/run/level_run_state.gd` | the pure model's invariants are enforced by the Node glue, not the model | FIXED | Verified real by reading `LevelSession.collect_wumpa()` (`src/gameplay/run/level_session.gd`): the "100 wumpa auto-summons a mask" economy rule (§4.2/§7.3, pure numbers — `wumpa_run` and `EconomyTuning.wumpa_mask_threshold`, zero Node/scene dependency) was implemented entirely in the Node glue via `run_state.record_wumpa_collected(amount)` followed by a raw `while run_state.wumpa_run >= mask_threshold: run_state.wumpa_run -= mask_threshold; _grant_mask(now_s)` loop that poked the model's field directly — `LevelRunState` itself had no way to compute or even guard this invariant (calling it headless with no Node glue, wumpa just accumulated forever past the threshold with no rollover and no `run_active` protection on the rollover step specifically). This is the one instance of the audit's general "invariants enforced by glue" claim that is both real and cleanly separable: the *decision* of how many masks a wumpa amount earns and what remains is pure arithmetic; only the *act* of granting a mask (informing the real `_player` Node, which owns its own invincibility timing) needs Node access. Added `resolve_wumpa_mask_rollover(mask_threshold: int) -> int` to `LevelRunState` — self-guarding on `run_active` and `mask_threshold <= 0` (mirroring every other model method's own precondition style rather than trusting the caller), decrementing `wumpa_run` per threshold crossed and returning the count. RED first: 3 new headless tests in `tests/gameplay/test_level_run_state.gd` calling the not-yet-existing method; verbatim RED: `Invalid call. Nonexistent function 'resolve_wumpa_mask_rollover (via call)' in base 'RefCounted (LevelRunState)'.` (18/21 passed). `collect_wumpa()` now calls `run_state.resolve_wumpa_mask_rollover(_economy.wumpa_mask_threshold)` and loops `_grant_mask(now_s)` exactly `masks_earned` times — same final `wumpa_run`, same call count and order relative to `_grant_mask` (which never reads `wumpa_run`), so observed behaviour is unchanged; confirmed via the existing glue-level integration suite `tests/gameplay/test_mercy_and_masks.gd` (11/11 unchanged, including `test_placed_wumpa_auto_summons_mask_and_rolls_counter`, the one that exercises this exact rollover through a real session). Two glue-side checks were examined and deliberately left in place, with reasons: (1) `_on_player_mask_state_changed`/`_grant_mask` still assign `run_state.masks` directly rather than through `record_mask_granted` — this bridges to the *player controller's own* live mask count (with its own invincibility/stacking logic), a genuinely different Node's authoritative state, not a pure-model invariant, so it stays; (2) `_next_checkpoint_id()` (already separately flagged as F2) stays in glue because it walks real scene-authored checkpoint links, which requires Node access by nature. Adjacent, not fixed: `record_mask_granted(amount)` (the model's own mask-increment method) has zero production callers — `_grant_mask`'s fallback path increments `run_state.masks` inline instead — a pre-existing dead-method gap, out of E9's scope since fixing it would touch the player-mask-sync path just discussed, not the wumpa/mask economy rule this finding is about. Full suite green: GUT 433/433 (4,168 asserts, +3 tests / +8 asserts over the 430/4,161 post-E8 baseline), Python 48/48, all 4 lints EXIT=0, `git diff --check` clean. Grepped every reference to `resolve_wumpa_mask_rollover` (new, only the two intended sites) and `run_state.wumpa_run` in `level_session.gd` (only the trailing `wumpa_changed.emit`, no remaining direct mutation). Files changed: `src/gameplay/run/level_run_state.gd`, `src/gameplay/run/level_session.gd`, `tests/gameplay/test_level_run_state.gd`. |
| E10 | `src/gameplay/run/level_run_state.gd` | `record_death(economy)` dereferences `economy` with no null guard | REJECTED | Read `record_death()`: it does dereference `economy.mercy_mask_death_threshold`/`economy.mercy_skip_death_threshold` with no null check — true as written. But traced every real call site to check reachability, per the brief: `record_death` has exactly one production caller, `LevelSession._record_death()` (`level_session.gd:357`, `run_state.record_death(_economy)`). `_record_death()` itself has exactly two callers: `record_player_death()` (a public method with **zero production callers anywhere in `src/`** — grepped `record_player_death` repo-wide, only test files call it directly) and `_on_player_respawned()`, which is only ever invoked because it is connected to the real player's `respawned` signal **inside `configure()`** (`level_session.gd:141-147`), i.e. it cannot fire before `configure()` has already run at least once. `_economy` is a `LevelSession` field assigned only in `configure()` (line 67, `_economy = economy`) and `refresh_tuning()` (line 214), both receiving `catalog.economy` from `tuning_service.catalog` (`game_root.gd:782` and `:873-878`). `catalog` cannot be null past `_ready()`'s boot gate (`if boot_error != OK: push_error(...); return`, `game_root.gd:104-106`) — `load_from_paths` only returns non-`OK`, leaving `catalog` unset, when the authored base resource itself fails to load or isn't a `GameplayTuning`; a live operator override additionally passes through `catalog_is_usable()` (which rejects a null `economy` section outright, `tuning_service.gd:145-148`) before ever being copied into `catalog`. The repo's actual, committed authored catalog (`data/tuning/gameplay.tres` → `economy`) has always had a populated `EconomyTuning` sub-resource — confirmed by the entire test suite reading real fields off it (`_catalog.economy.mercy_skip_death_threshold` etc., hundreds of passing asserts). No code path in the current, real codebase constructs or forwards a null economy into this chain. Sibling methods that touch `_economy` from Node glue (`collect_wumpa`, `_physics_process`) do carry their own `_economy == null` guards; `_record_death()`/`record_death()` is the one link in the chain without one, but reaching it with a null economy would require an authoring-time defect (a `gameplay.tres` edit that omits the `economy` sub-resource while still loading as a valid `GameplayTuning`) rather than a code call-site defect — a different failure category the brief's "real call site" test is asking me to exclude. No test added (nothing to make red — there is no reachable defect to reproduce), no code changed. Full verification block re-run and unchanged: GUT 433/433 (4,168 asserts), Python 48/48, all 4 lints EXIT=0, `git diff --check` clean. |
| E11 | `src/gameplay/run/level_run_state.gd` | `relic_tier` can go stale against `best_relic_time_ms` | FIXED | Verified real by reading the persistence chain: `LevelRunState.record_level_complete()`/`relic_tier_for_time()` are internally consistent (both derived from the same `relic_elapsed_s()` float in the same call, nothing stale within this file — it holds no "best" concept at all, only the current run's tier). The real defect is one layer downstream, in `SaveModel.improved_relic_record()` (`src/core/save_model.gd`), which stored a caller-supplied `tier: StringName` alongside `best_relic_time_ms` instead of deriving it — two independently-writable fields that can drift apart via **two** distinct, real mechanisms: (1) **millisecond rounding**: `relic_tier` is computed from the raw float `elapsed_s` (seconds) via `relic_tier_for_time`, while `best_relic_time_ms` is `roundi(elapsed_s * 1000)` — near a par boundary (e.g. `elapsed_s = 60.0004s` against a `60.0s` platinum par) the raw-seconds tier check can disagree with what the *rounded, persisted* millisecond value would say if re-checked; (2) **par re-tuning** (explicitly anticipated by 01-DESIGN Q12/H9 — "re-tuned after friends play"): `improved_relic_record` only touches a record when a *new* best is set, so a previously-stored tier from before a re-tune is never refreshed, even on a later non-improving completion for that same level. Currently latent only because `n_sanity_beach.tres` ships `relic_*_s = 0.0` (Q12), so `relic_pars_authored()` locks relic mode out entirely and nobody can earn a tier yet — but the mechanism is real and must not wait for pars to exist. Fix: `improved_relic_record(record, elapsed_s, level_meta: LevelMeta)` (was `tier: StringName`) now **always** recomputes `relic_tier` from whichever `best_relic_time_ms` ends up stored (new or pre-existing), via `LevelRunState.relic_tier_for_time(best_ms / 1000.0, level_meta)` — the same canonical function the model itself uses for a fresh run's tier — so tier can never independently disagree with the time it describes, and every completion (improving or not) self-heals the stored tier against the level's *current* pars. `ResultsModel.persisted_profile()` gained a required `level_meta: LevelMeta` parameter (dropped the now-unused `relic_tier` payload read) and validates `level_meta.level_id == level_id` before trusting it, so a caller can never apply the wrong level's pars to another level's stored best. RED first: added `test_persisted_relic_tier_is_always_derived_from_best_time` (`tests/gameplay/test_relic_math.gd`) calling the new 3-arg `persisted_profile` with a payload whose time qualifies for platinum but whose `relic_tier` field lies and says "gold"; verbatim RED (a parse-time failure since the call is direct, not `.call()`): `SCRIPT ERROR: Parse Error: Too many arguments for "persisted_profile()" call. Expected at most 2 but received 3.` / `ERROR: Failed to load script "res://tests/gameplay/test_relic_math.gd" with error "Parse error".` (whole file failed to load). After the fix the test asserts the persisted `relic_tier` is "platinum" (derived), not "gold" (the lying payload value) — 17/17 in that file. Grepped every call site of both changed signatures: `improved_relic_record` — only caller is `results_model.gd:151` (updated); `persisted_profile` — `game_root.gd:350` (production, `meta` was already in scope from the preceding `results_model.build(results, meta, ...)` call two lines above) and 5 test call sites across `tests/gameplay/test_relic_math.gd` (4) and `tests/gameplay/test_results_model.gd` (1), all updated to pass their existing `_meta` fixture. Full suite green: GUT 434/434 (4,170 asserts, +1 test / +2 asserts over the 433/4,168 post-E9 baseline — E10 was a ledger-only REJECTED with no test), Python 48/48, all 4 lints EXIT=0, `git diff --check` clean. Files changed: `src/core/save_model.gd`, `src/gameplay/run/results_model.gd`, `src/core/game_root.gd`, `tests/gameplay/test_relic_math.gd`, `tests/gameplay/test_results_model.gd`. Note: the finding's root cause sits in `save_model.gd`/`results_model.gd`, not `level_run_state.gd` itself (which has no "best" concept to go stale) — `level_run_state.gd` was untouched; its `relic_tier_for_time()` is the canonical function the fix now reuses for persistence instead of duplicating. |
| B11 | `src/core/game_root.gd` | design §4.4 lists app-pause as a profile write trigger; `store_profile` is never called on app pause | OPEN | |
| B12 | `src/core/save_service.gd` | `recovered_from_backup` is set and read by nothing — a rollback is invisible | OPEN | |
| J3 | `src/gameplay/player/player_controller.gd` | a locked `ACTION_PHASE` press is not consumed and can fire on the frame the gate opens | FIXED | Verified real by reading `advance_logic`: the whole condition was `_phase_enabled and _input_tuning.phase_button_unlocked and _intents.consume_pressed(...) != null` — GDScript's `and` short-circuits, so whenever `_phase_enabled` (or `phase_button_unlocked`) was false, `consume_pressed` was never called and a buffered PHASE press stayed queued in `InputIntentBuffer`, which `configure()` never clears (only `respawn()` does). The real path this reaches: `game_root.gd`'s tuning-refresh reconfigure (`_apply_active_level_tuning`, called on any live tuning reload) re-invokes `player.call("configure", ..., _phase_available())` with a freshly re-read `_phase_available()` mid-level, on the same long-lived `InputIntentBuffer` (`router.get("buffer")`) — so a press buffered while locked can still be sitting in the queue (within `action_buffer_s`, 0.15s authored) the instant a later `configure()` call flips `_phase_enabled` true, firing `PhaseState.request_toggle` on that frame. RED first: added `test_phase_press_buffered_while_locked_does_not_fire_once_the_gate_opens` (`tests/gameplay/test_player_controller.gd`) — configures the controller locked, pushes a PHASE press, advances logic (asserts no toggle), then reconfigures with `phase_enabled=true` (simulating the gate opening) and advances logic again inside the same buffer window; verbatim RED: `[STRING_NAME(orange)] expected to equal [STRING_NAME(blue)]:  a press buffered while PHASE was locked must not leak through the instant the gate opens` (at the old line 515). Fix: hoist `_intents.consume_pressed(InputIntent.ACTION_PHASE, ...)` out of the `and` chain into its own statement, always called once per frame regardless of gate state (draining/discarding a locked press instead of leaving it queued), then gate only the *action* (`PhaseState.request_toggle`) on `_phase_enabled and phase_button_unlocked and phase_press != null` — same pattern already used by `_apply_jump_release`'s unconditional `consume_released`. Grepped every reference to `InputIntent.ACTION_PHASE` and `consume_pressed`: the only production consumer of `ACTION_PHASE` via `consume_pressed` is this one call site (`player_controller.gd:253`); `consume_pressed`'s own signature is unchanged, so no other call site needed updating. Full suite green: GUT 435/435 (4,173 asserts, +1 test / +2 asserts over the 434/4,170 baseline), Python 63/63, all 4 lints EXIT=0, `git diff --check` clean. One full-suite run hit an unrelated, non-reproducing failure in `test_wall_run_state.gd`'s real-physics wall-attach test (a timing-sensitive `await wait_physics_frames` test, not touching PHASE or any file this fix changed) — passed 3/3 in isolation and the very next full run was 435/435 clean; a load-induced timing flake under the concurrently-running suite box, not a regression from this fix. Files changed: `src/gameplay/player/player_controller.gd`, `tests/gameplay/test_player_controller.gd`. |
| J4 | `src/gameplay/player/player_controller.gd` | new gating params default to `false`/`null`, defeating the compiler backstop behind "grep every call site" | FIXED | Verified real by reading `configure()`: `economy_tuning: EconomyTuning = null` and `phase_enabled: bool = false` both had defaults, so a call site that forgets to pass either compiles and runs fine, silently wiring a live level with no economy and/or PHASE permanently locked, instead of the missing wiring becoming a parse error at every other call site the moment the signature is grepped and touched. A repo-wide scan (every literal `"configure", <9-tuning-shaped args>` call plus one dynamic `.callv()` builder) found 13 call sites genuinely depending on one or both defaults: 12 relying on both (`test_swing_pendulum.gd`:739,769, `test_camera_archetypes.gd`:659,835, `test_grind_state.gd`:778,822, `test_depth_prediction.gd`:369,415, `test_player_controller.gd`:186,692, `test_wall_run_state.gd`:553,775) and 1 relying on `phase_enabled` alone (`test_player_controller.gd`'s `_new_controller()` helper, used by most tests in that file) — proving this is a live gap, not a hypothetical one. RED first: added `test_configure_gating_params_have_no_silent_default` (`tests/gameplay/test_player_controller.gd`) — configures a controller fully (`phase_enabled=true`, real `_economy`), then `callv`s it with the pre-Task-16 7-arg shape and asserts (a) that call now raises a real engine error (`assert_engine_error("Method expected", ...)`, which also marks it handled so GUT doesn't fail the run on the expected error) and (b) `_phase_enabled`/`_economy_tuning` are unchanged by the rejected call, i.e. it never silently re-applied `false`/`null`; verbatim RED (3 failures): `Expected engine error containing 'Method expected'.` plus `[false] expected to equal [true]` and `[<null>] expected to equal [...EconomyTuning...]` — proving today's call silently succeeds and overwrites both fields. Fix: removed both defaults (`economy_tuning: EconomyTuning`, `phase_enabled: bool`, both now required), then updated all 13 call sites found above to pass explicit values (`null`/`false` for the 12 unrelated to economy/phase, `false` for the phase_enabled-only site) plus `tests/gameplay/test_mercy_and_masks.gd`'s `_configure_player()` dynamic `.callv()` builder (missed by the literal-call scan since its args array is built at runtime) to always append a `false` phase_enabled alongside its existing conditional economy append. The 8 already-fully-explicit call sites (`game_root.gd`:737,847, `warp_room.gd`:91, `phase0_game.gd`:76,137, `test_phase_state_integration.gd`:117, `test_player_controller.gd`:417,460 — already passing 9 args, including the J3 test added this session) needed no change. Re-ran every touched test file individually (`test_player_controller` 19/19, `test_swing_pendulum` 22/22, `test_camera_archetypes` 20/20, `test_grind_state` 22/22, `test_depth_prediction` 17/17, `test_wall_run_state` 15/15, `test_mercy_and_masks` 11/11, `test_phase_state_integration` 6/6). Full suite green: GUT 436/436 (4,179 asserts, +1 test / +6 asserts over the 435/4,173 post-J3 baseline), Python 63/63, all 4 lints EXIT=0, `git diff --check` clean. Files changed: `src/gameplay/player/player_controller.gd`, `tests/gameplay/test_player_controller.gd`, `tests/gameplay/test_swing_pendulum.gd`, `tests/gameplay/test_camera_archetypes.gd`, `tests/gameplay/test_grind_state.gd`, `tests/gameplay/test_depth_prediction.gd`, `tests/gameplay/test_wall_run_state.gd`, `tests/gameplay/test_mercy_and_masks.gd`. |
| J5 | git history | all 16 commits authored by generic `root` | OPEN | |
| J6 | `src/ui/safe_area_control.gd` | unplanned file — not in 02-PLAN's file list | OPEN | |
| J9 | `build/` | a stale `.idsig` sits beside the APK | OPEN | |
| J10 | `android/` | a debug keystore is committed (predates this range) | OPEN | |
| J14 | `handover/phase1/` | the entire Phase 1 authorisation package is untracked | OPEN | |
| G8 | `scripts/lint_level_authoring.py` | spine ≠ playable extent | FIXED | Verified real by reading `_checkpoint_findings`: the checkpoint-spacing rule treats the spine's own first/last points as the level's start/finish boundaries (`boundaries = sorted([0.0, *checkpoint_distances, cumulative[-1]])`), but nothing ties those endpoints to the level's actual playable edges — the player's real spawn (`Player`, script `player_controller.gd`) and the real finish trigger (`LevelSession.configure()` requires a root `Finish` Area3D, `level_session.gd:163-169`, so it's a runtime-enforced anchor, not a convention). A spine that stops short of either real edge leaves that stretch invisible to pacing. RED first: added `test_spine_stopping_short_of_the_finish_still_fires_spacing` (`tests/lint/test_level_authoring_lint.py`) against a new fixture (`level_spine_extent_gap_bad.tscn`) with a 50 m spine but a root `Finish` 400 m from the spine start (350 m past the spine's own end — 88.9 s of untracked distance at the fixture's 4.5 m/s pace, limit 60 s); verbatim RED: `AssertionError: Lists differ: [] != ['checkpoint_spacing']`. Fix: added `_spine_extent_gaps(nodes, spine)`, which finds the root `Player`/`Finish` nodes when present (by `script_path`/`node_type`+`path`, the same identification style already used for camera regions and crates) and returns the straight-line residual from spine-start to `Player` and spine-end to `Finish`; `_checkpoint_findings` now folds these into the boundary list (`-head_gap` and `cumulative[-1] + tail_gap`) instead of the bare `0.0`/`cumulative[-1]`. No new constant: reuses the existing `checkpoint_spacing_limit_s`/`design_pace_mps` threshold already applied to every other interval. Fully backward compatible when no root `Player`/`Finish` exists (both gaps default to `0.0`, identical to prior behavior) — confirmed no existing fixture in `tests/lint/test_level_authoring_lint.py` has a root-level `Finish` Area3D or a `player_controller.gd`-scripted node (grepped both), so no other test's expectations moved. Real level check: `wr1_n_sanity_beach.tscn`'s spine starts 0.05 m from `Player` and ends 6.19 m from `Finish` — both folded in, `test_real_n_sanity_beach_level_passes_the_authoring_lint` still green. Full verification: GUT 434/434 (4,170 asserts, unchanged), Python 49/49 (+1 over the 48 baseline), all 4 lints EXIT=0, `git diff --check` clean. No production signature changed apart from the new pure helper `_spine_extent_gaps` (no existing callers, so nothing to grep). Files changed: `scripts/lint_level_authoring.py`, `tests/lint/test_level_authoring_lint.py`, `tests/fixtures/level_spine_extent_gap_bad.tscn`. |
| G9 | `scripts/lint_level_authoring.py` | spine ordered by document order | FIXED | Verified real by reading `_checkpoint_findings`: the spine's traversal order is `sorted(..., key=lambda node: node.order)`, where `order` is the flatten pass's document/declaration-order counter — nothing cross-checks that declaration order matches real spatial order, so a segment (or a marker within one) authored out of sequence (a reordered instance line, a copy-paste) silently corrupts every checkpoint's distance-along-the-route with no error. RED first: added `test_spine_marker_declared_out_of_spatial_order_fires_its_rule` (`tests/lint/test_level_authoring_lint.py`) against a new fixture (`level_spine_order_bad.tscn`: `Start`(0)→`Overshoot`(-100)→`Doubleback`(-50), declared in that order — `Doubleback` sits spatially between the other two but is declared last); verbatim RED (import, since the test also needed the new rule constant): `ImportError: cannot import name 'SPINE_ORDER_RULE' from 'scripts.lint_level_authoring'`. Fix: new `SPINE_ORDER_RULE` + `_spine_order_findings(scene_name, spine)`, which projects every spine marker onto the chord from the spine's own first point to its own last point and asserts declared order is non-decreasing progress along that chord (`math.isclose`-guarded, matching the file's existing float-comparison style); flags any marker whose progress is behind an earlier-declared one. Wired into `_checkpoint_findings` right after the existing `len(spine) < 2` guard, threading the result through the function's existing early-return paths (`findings + [...]`) instead of the previous single-`return` style, so the check applies even when a later guard (pace/zero-length) would otherwise cut off first. Verified all 7 real segments in `wr1_n_sanity_beach.tscn` author their Spine markers with strictly decreasing Z in both document and world order (checked every segment file by hand), so the concatenated 29-point spine is already monotonic — `test_real_n_sanity_beach_level_passes_the_authoring_lint` stays green. Full verification: GUT 434/434 (4,170 asserts) on 2 of 3 runs, 1 unreproducible red (433/434, no GDScript touched by this change so it cannot be the cause — matches the pre-documented N2 flake, not re-run into a "fix"); Python 50/50 (+1 over the 49 baseline), all 4 lints EXIT=0, `git diff --check` clean. Grepped every reference to `_checkpoint_progression_findings`'s call site (the one place `findings` used to be assigned rather than extended) — now the sole caller, updated in place; no other function signature changed. Files changed: `scripts/lint_level_authoring.py`, `tests/lint/test_level_authoring_lint.py`, `tests/fixtures/level_spine_order_bad.tscn`. |
| G10 | `scripts/lint_level_authoring.py` | no off-spine projection check | FIXED | Verified real by reading `_project_onto_polyline`: it computes `best_distance` (the checkpoint's real perpendicular distance to its nearest spine segment) internally but discards it, returning only `best_along` — the "distance so far" value `_checkpoint_findings` trusts for every checkpoint has never been checked against how far off the spine the checkpoint actually sits. §5.2 of the design doc says off-spine excursions "always mean something" for crates/wumpa, but a checkpoint anchors pacing and respawn, so it cannot legitimately be one. RED first: added `test_checkpoint_far_off_the_spine_fires_its_own_rule` (`tests/lint/test_level_authoring_lint.py`) against a new fixture (`level_checkpoint_off_spine_bad.tscn`: a 100 m spine, one authored CameraRegion enclosing it, and a checkpoint 50 m to the side) plus its own meta (`level_checkpoint_off_spine_meta.tres`, `crate_count=1`, since checkpoints count toward `LevelMeta.crate_count`); verbatim RED: `ImportError: cannot import name 'CHECKPOINT_OFF_SPINE_RULE' from 'scripts.lint_level_authoring'`. Fix: new `CHECKPOINT_OFF_SPINE_RULE` + `_checkpoint_off_spine_findings`, which reuses the level's own authored per-segment CameraRegion geometry (via a new `_camera_region_bounds(nodes)` helper wrapping the existing `_collision_bounds`/`Bounds.contains`, the same enclosure check rule (c) already uses for required jumps) rather than inventing a numeric tolerance — a checkpoint not enclosed by any authored CameraRegion is off-spine. Skips the check entirely when a scene authors no CameraRegion at all (so narrow single-rule fixtures with checkpoints but no regions, e.g. `level_checkpoint_progression_bad.tscn`, are unaffected — grepped every fixture with a `checkpoint` crate to confirm none has a CameraRegion). Verified both real checkpoints in `wr1_n_sanity_beach.tscn` (crate 14, crate 30) sit inside their own segment's CameraRegion bounds by hand; `test_real_n_sanity_beach_level_passes_the_authoring_lint` stays green. Full verification: GUT 434/434 (4,170 asserts), Python 51/51 (+1 over the 50 baseline), all 4 lints EXIT=0, `git diff --check` clean. Grepped every call site of `_collision_bounds` (unchanged signature): the pre-existing one in `_required_jump_findings` (`scripts/lint_level_authoring.py:688`) and my new one — both correct. Files changed: `scripts/lint_level_authoring.py`, `tests/lint/test_level_authoring_lint.py`, `tests/fixtures/level_checkpoint_off_spine_bad.tscn`, `tests/fixtures/level_checkpoint_off_spine_meta.tres`. |
| G11 | `scripts/lint_level_authoring.py` | region bounds ignore rotation/scale | REJECTED | Read `Bounds.contains()` and traced every transform it depends on: it inverts the region's FULL composed basis (`_basis_inverse_xform`, a general cofactor/Cramer's-rule 3x3 inverse valid for any invertible matrix — rotation, non-uniform scale, or both) before comparing to `half_size`, and `_collision_bounds` builds that `Bounds` from `child.world_transform` — the CollisionShape3D's fully-composed transform, which folds in every ancestor's rotation and scale via `_compose_transform`/`_basis_multiply` up the whole chain. This is not naive axis-aligned math; it is a real oriented-box (OBB) test. Verified two ways rather than trusting the reading alone, since the audit has been wrong twice already this session: (1) independently ran the real Godot 4.7.1 binary (`godot --headless --path`) against a tiny probe scene using the exact same `Transform3D(0,0,-1,0,1,0,1,0,0,0,2,0)` string as the repo's own existing `level_transform_hierarchy.tscn` fixture, and confirmed real Godot's `basis.x/y/z` and a child's `global_position` match the lint's `_parse_transform`/`_compose_transform` output exactly (both give child world position `(10,2,2)` for `TransformParent/TransformChild`) — the parsing convention is correct, not transposed as I first suspected from a naive column-major reading; (2) directly exercised `Bounds.contains()` in a Python REPL with a region rotated 90° about Y *and* non-uniformly scaled `(2,1,1)`, using both the `rotation_degrees`+`scale` property path and the packed `transform` path — a world point exactly on the true (rotated, scaled) boundary reports `True`, and a point 0.1% further out reports `False`, in both cases. `test_flatten_composes_transform_basis_rotation_and_scale` (already existing, already passing) additionally covers all five transform-authoring forms (`transform=`, `basis=`, `rotation=`, `scale=`, combined XYZ Euler) with precise expected positions. No mechanism ignores rotation or scale anywhere in this chain. No code or test changed — ledger-only commit, per the I9/E10 precedent for a rejection with no red step to record. Full verification block re-run and unchanged: GUT 434/434 (4,170 asserts), Python 51/51, all 4 lints EXIT=0, `git diff --check` clean. |
| G12 | `scripts/lint_level_authoring.py` | overlap resolution differs from runtime | FIXED | Verified real by reading the runtime: `CameraRailController.update_camera()` (`src/gameplay/camera/camera_rail_controller.gd:154-169`) collects every currently-overlapping region's offset (`_active_regions`, maintained by real `body_entered`/`body_exited` Area3D signals) into `region_offsets` and passes ALL of them to `CameraBlend.resolve_offset()` (`camera_blend.gd:5-15`), which returns the plain average of the whole set — never a single "best" region. The lint's `_required_jump_findings`, by contrast, tried each enclosing region independently and accepted the jump if *any one* individually cleared the depression threshold (`if depression >= ...: passes = True; break`) — a model the real camera never produces once two regions' bounds overlap the same jump, since the runtime would average them, not pick one. RED first: added `test_overlapping_regions_are_blended_like_the_runtime_camera` (`tests/lint/test_level_authoring_lint.py`) against a new fixture (`level_required_jump_overlap_blend_bad.tscn`: same takeoff/landing/rail geometry as the existing `level_required_jump_bad.tscn` baseline, but with two fully-overlapping CameraRegions, `grind` (15.068° alone — clears the 15° minimum) and `wall_run` (3.302° alone — fails); their real blended average yields 7.306°, well under the minimum); verbatim RED: `AssertionError: Lists differ: [] != ['required_jump_depression']` (the buggy code accepted the jump via the grind region alone). Fix: new `_blend_camera_offsets(offsets)` (a direct port of `CameraBlend.resolve_offset`'s pure averaging, minus the empty-list/default-offset branch since the call site is already guarded non-empty by the existing `if not matching_regions` check) computes ONE blended offset from every matching region's already mode-adjusted offset (still applying the existing per-region `wall_run` component swap first, matching the runtime's own per-region-then-average order), then a single camera position/depression is computed and checked — replacing the existential per-region loop entirely. Verified the real level and every pre-existing fixture are single-region cases (grepped every fixture and the real segment for `type="Area3D"` camera-region counts touching a `RequiredJump`), so averaging over one element is a no-op and none of them changed behavior; `test_required_jump_fires_the_depression_rule`, `test_wall_run_camera_mode_cannot_bypass_jump_depression`, and the other 3 required-jump tests all stayed green untouched. Full verification: GUT 434/434 (4,170 asserts) on 2 of 3 runs, 1 unreproducible red (433/434, no GDScript touched by this change so it cannot be the cause — matches the pre-documented N2 flake, not chased); Python 52/52 (+1 over the 51 baseline), all 4 lints EXIT=0, `git diff --check` clean. Grepped every call site of `_camera_position`/`_runtime_camera_offset` (both unchanged signatures, single call site each, now feeding the blended offset instead of a per-region one) and the new `_blend_camera_offsets` (one caller). Files changed: `scripts/lint_level_authoring.py`, `tests/lint/test_level_authoring_lint.py`, `tests/fixtures/level_required_jump_overlap_blend_bad.tscn`. |
| G13 | `scripts/lint_level_authoring.py` | parser drops unclassifiable lines silently | FIXED | Verified real by reading `_parse_scene`: any section header whose name isn't `ext_resource`/`sub_resource`/`node` (e.g. `[editable path=...]`, `[connection ...]`, or any future Godot syntax) sets `current_properties = None` and is silently skipped with a bare `continue`; and any content line that doesn't match `PROPERTY_PATTERN`, or does but has no active `current_properties` (inside such an unrecognized section, or directly after a header like `ext_resource`/`gd_scene` that never sets one), is silently dropped by the `if match is not None and current_properties is not None` guard — the same failure class as the audit's P0-1 (a condition swallowing real content instead of surfacing it). Confirmed this isn't hypothetical: `scenes/segments/seg_phase_gauntlet.tscn` genuinely authors `[editable path="..."]` sections today. Per the brief, the fix is to fail loudly, not to log. RED first: two new fixtures/tests in `tests/lint/test_level_authoring_lint.py` — `test_parser_fails_loudly_on_an_unrecognized_scene_section` (`level_unclassifiable_section_bad.tscn`, an `[editable path="Start"]` section, the exact real-world shape) and `test_parser_fails_loudly_on_a_line_it_cannot_classify` (`level_unclassifiable_line_bad.tscn`, a stray `mystery_field = 5` line before any section); verbatim RED (both): `AssertionError: ValueError not raised`. Fix: two new sets, `KNOWN_SCENE_SECTIONS = {ext_resource, sub_resource, node}` (sections with real handling) and `INERT_SCENE_SECTIONS = {gd_scene, gd_resource}` (the deliberately content-free top-of-file declaration, the only header form safe to skip outright); any OTHER section name now raises `ValueError` naming the file and section. Any non-blank, non-comment, non-header line that either doesn't match `PROPERTY_PATTERN` or has no active `current_properties` also now raises, naming the exact offending line — this replaces the previous silent no-op for both failure shapes. Checked whether any currently-scanned scene trips the new loud path, per the brief's explicit instruction: it does not — `python3 scripts/lint_level_authoring.py` (the real, unmodified CLI over the real repo) still exits 0, and all 25 pre-existing lint tests including `test_real_n_sanity_beach_level_passes_the_authoring_lint` and `test_warp_room_is_not_misclassified_as_a_collectible_level` stayed green, because `seg_phase_gauntlet.tscn` is reachable only through the exempt, never-flattened `phase05_gauntlet.tscn` (`LEVEL_META_EXEMPT_SCENES`). **Adjacent finding, not fixed, reported per the brief:** deliberately calling `_parse_scene` on `scenes/segments/seg_phase_gauntlet.tscn` directly confirms it *would* trip the new loud path (`unrecognized scene section [editable]`) the moment it becomes reachable — e.g. if Task 17 ever wires Phase 0.5 gauntlet content back into a non-exempt level. This is out of scope here (Task 17/Wave B) and is not itself a defect introduced by this fix; it is the new safety net correctly identifying a real, previously-invisible gap the moment it would matter. Full verification: GUT 434/434 (4,170 asserts), Python 54/54 (+2 over the 52 baseline), all 4 lints EXIT=0, `git diff --check` clean. Grepped every call site of `_parse_scene` (`find_authoring_violations`, `_flatten_into`, both unchanged signatures) — behavior-only change, no caller needed updating. Files changed: `scripts/lint_level_authoring.py`, `tests/lint/test_level_authoring_lint.py`, `tests/fixtures/level_unclassifiable_section_bad.tscn`, `tests/fixtures/level_unclassifiable_line_bad.tscn`. |
| G14 | `tests/lint/test_level_authoring_lint.py` | rule-edge test gaps | FIXED | Verified real by cross-referencing every `AuthoringViolation(...)` construction site in the lint against every fixture/detail string in the test file: 8 of 16 distinct violation branches across the four authored rules had zero test or fixture coverage — `grep`-ing each one's literal detail text against `tests/` came back empty for all 8. Added one fixture + one test per gap, each isolating exactly the one branch (verified by dry-running every new fixture through the current lint before writing assertions, confirming each produces exactly the single intended finding): (1) `level_spine_too_short_bad.tscn` — spine with only 1 Marker3D (`"at least two ordered Spine Marker3D nodes"`); (2) `level_zero_pace_bad.tscn` + new `level_zero_pace_meta.tres` (`design_pace_mps = 0.0`) — (`"design_pace_mps must be positive"`); (3) `level_spine_zero_length_bad.tscn` — two coincident spine markers (`"non-zero authored distance"`); (4) `level_checkpoint_final_link_bad.tscn` — two checkpoints, correct pairwise link, but the spatially *final* one illegally sets `next_checkpoint_id` (`CHECKPOINT_PROGRESSION_RULE`'s "final checkpoint...must not link" branch, previously exercised by no fixture, including the real level, at all); (5) `level_required_jump_missing_children_bad.tscn` — a RequiredJump with a Takeoff but no Landing child; (6) `level_required_jump_no_region_bad.tscn` — valid Takeoff/Landing/Rail but zero CameraRegions in the scene; (7) `level_required_jump_no_rail_bad.tscn` — enclosed by a region but the Rail has only 1 Marker3D (no usable polyline); (8) `level_crate_missing_id_bad.tscn` + new `level_one_crate_meta.tres` — a crate instance with `crate_id` never authored (`CRATE_ID_RULE`'s "missing/invalid" branch, previously exercised only by the *duplicate*-id case). No production bug behind any of the 8 — each branch already behaves correctly, this is coverage only (same shape as I9/I10/A8) — so proved every one mutation-sensitive rather than trusting the reading: for each, temporarily neutralized its exact guard condition in `scripts/lint_level_authoring.py` (`cp`-backed, one at a time, restored via `cp` after each and confirmed `diff` byte-identical to the pristine file before the next), and confirmed the *specific* new test goes red each time: (1) wrong finding (falls through to the zero-length-distance branch instead), (2) `ZeroDivisionError` (the removed guard was the only thing preventing a divide-by-zero pace), (3) wrong finding (empty list — the interval collapses to zero), (4) wrong finding (empty list — no progression violation reported), (5) wrong finding (falls through to the "not enclosed by one camera region" branch, since this fixture also authors no CameraRegion), (6) `ZeroDivisionError` (an empty `matching_regions` reaching `_blend_camera_offsets` unchecked), (7) `TypeError` unpacking `None` (a missing rail's `camera_frame` reaching the tuple-unpack unchecked), (8) wrong finding (empty list — a `None` crate_id silently becomes a valid dict key). Test 1 was tightened mid-verification: the rule-name-only assertion (`assertEqual([...], [CHECKPOINT_SPACING_RULE])`) was not actually branch-specific, since a 1-marker spine also independently trips the zero-length-distance branch under the same rule name — added an `assertIn` on the exact detail text so the mutation proof is genuine. Full verification: GUT 434/434 (4,170 asserts), Python 62/62 (+8 over the 54 baseline), all 4 lints EXIT=0, `git diff --check` clean. Test-only change plus 8 new fixtures and 2 new meta resources; no production signature touched, no call sites to grep. Files changed: `tests/lint/test_level_authoring_lint.py`, `tests/fixtures/level_spine_too_short_bad.tscn`, `level_zero_pace_meta.tres`, `level_zero_pace_bad.tscn`, `level_spine_zero_length_bad.tscn`, `level_checkpoint_final_link_bad.tscn`, `level_required_jump_missing_children_bad.tscn`, `level_required_jump_no_region_bad.tscn`, `level_required_jump_no_rail_bad.tscn`, `level_one_crate_meta.tres`, `level_crate_missing_id_bad.tscn`. |
| C11 | `src/core/game_root.gd` | `NOTIFICATION_WM_CLOSE_REQUEST` is untested — removing it costs nothing | OPEN | |
| C16 | `src/core/game_root.gd` | §4.4's several write triggers collapse to one `store_profile` call site | OPEN | |
| C17 | `src/gameplay/player/player_controller.gd` | `configure`'s new params default, blunting Task 16's own discipline | REJECTED (already fixed) | Verified by reading the code and by git history: this is the identical finding to J4 above, not a distinct bug — `configure()` has exactly one pair of defaulted params anywhere in this file (`economy_tuning: EconomyTuning = null`, `phase_enabled: bool = false`), confirmed both by reading the full file (grepped every `func`/`static func` signature; no other parameter in the file carries a default) and by `git show 5e49d77` (Task 16's own commit, "Restore progressive PHASE gating at both consumption sites"), which added `phase_enabled: bool = false` onto the pre-existing `economy_tuning: EconomyTuning = null`. J4 (commit `c2af04d`, this same fix run) already removed both defaults, made the params required, and updated all 13 call sites that depended on either default, with a mutation-provable red/green test (`test_configure_gating_params_have_no_silent_default`). Re-reading `configure()`'s signature now shows `economy_tuning: EconomyTuning` and `phase_enabled: bool` with no defaults — C17's claim no longer matches the code, and there is no second location it could refer to. No red step to record (no bug remains to reproduce) and no code changed — ledger-only commit, per the I9/E10/G11 precedent for a rejection with no red step. Full verification block re-run and unchanged: GUT 436/436 (4,179 asserts), Python 63/63, all 4 lints EXIT=0, `git diff --check` clean. |
| C18 | `src/ui/hud.gd`, `src/core/game_root.gd` | stale mercy banner across runs; hub touch exclusions omit the drawer; retry-from-pause round-trips a full hub instantiate | FIXED (a REJECTED, b FIXED, c FIXED) | Three genuinely distinct defects the audit grouped into one row — treated as three sub-fixes in one commit per the brief, but (a)/(b)/(c) would each have deserved their own audit row (different files, different mechanisms, independently reproducible). (a) stale mercy banner: **REJECTED as already fixed** by `eb75384` ("Clear mercy notices without hiding skip offers"), which added `_clear_mercy_panel()` to `HUD.configure()` (called by every authored-level entry via `set_active_level_session`) and to `set_run_display_visible(false)` (the toybox path). Traced every way `GameFlow.State.LEVEL` can be entered — fresh portal, quit-then-reselect, retry-from-results, retry-from-pause, session-resume-at-boot, toybox — and every one already routes through one of those two clears. `test_new_real_run_clears_previous_mercy_banner` (`test_island_slice.gd`) already covers the quit-to-hub path and passes; wrote two throwaway (never committed) probes for the two untested paths — retry-from-pause and retry-from-results — both passed against unmodified code, confirming no live gap. No red step exists because there is no bug to reproduce. (b) hub touch exclusions omit the debug drawer: **verified real and FIXED.** `WarpRoom._configure_player()` hardcoded `touch.set_touch_exclusion_controls([$UI/LevelList])`, never including the `TuningDebug` HUD strip/Drawer that `GameRoot._level_touch_exclusions()` already excludes for the LEVEL state — so in a debug build, a tap on the always-visible debug HUD strip or an open Drawer over the hub would also register as hub movement/jump, the same class of bug I8 already fixed for the level HUD. RED first: added `test_hub_touch_exclusions_include_the_debug_drawer` (`tests/integration/test_warp_room.gd`) — verbatim RED: two failures, `"the hub's touch controls must exclude the debug HUD strip..."` and `"...the debug drawer..."` (0/1 passed, 4/6 asserts). Fix: extracted `GameRoot._debug_touch_exclusions()` (used by both `_level_touch_exclusions()` and the hub), added an `extra_touch_exclusions: Array = []` parameter to `WarpRoom.configure()` (default preserves the 2 existing 5-positional-arg test call sites in `test_warp_room.gd`), concatenated into `touch.set_touch_exclusion_controls()`, and wired `_render_warp_room()`/`_refresh_warp_room_tuning()` to pass `_debug_touch_exclusions()`. Grepped every `WarpRoom.configure()` call site: 2 in `tests/integration/test_warp_room.gd` (both unaffected by the new defaulted param), 2 in `game_root.gd` (both updated). (c) retry-from-pause round-trips a full hub instantiate: **verified real; judged a genuine (if minor) defect, not merely inelegant, and FIXED with the narrowest available mechanism.** `_on_pause_retry_requested()` called `_select_level()`, which for this path dispatches RESUME → QUIT_LEVEL → PORTAL_ENTER — the middle transition (LEVEL→WARP_ROOM) fully instantiates `WARP_ROOM_SCENE` (Player, TouchControls, InputRouter, GamepadInput, portals, `_ready()`/`configure()` signal wiring) only to have the very next dispatch's `_clear_content()` disable and queue-free it before a frame ever renders — real, non-trivial CPU work (packed-scene instantiation + N nodes' `_ready()`) thrown away on every single retry, on the Android hardware this ships to. Considered and rejected the alternative of teaching `GameFlow`'s FSM a direct PAUSED→LEVEL retry transition (mirroring the existing RESULTS→LEVEL retry): traced `GameRoot._render_state()`'s `previous_state == PAUSED` branch, which early-returns (UI-sync only, no content reload) because today PAUSED→X is *always* a plain resume — collapsing retry into that same transition would silently skip reloading the level, a real regression risk in a delicate, exhaustively-tested area (`tests/core/test_game_flow.gd`). Instead added a private `_suppress_next_warp_room_render` flag, set immediately before and unconditionally cleared immediately after the `_select_level()` call in `_on_pause_retry_requested()` (so it can never leak past this one call regardless of which internal path `_select_level` takes), checked at the top of `_render_warp_room()` to skip the instantiate for exactly this one transient waypoint. RED first: added `test_pause_overlay_retry_does_not_round_trip_a_hub_instantiate` (`tests/integration/test_main_boot.gd`), driven by a new `warp_room_instantiate_count()` counter (same observability pattern as the existing `threaded_level_poll_count()`) incremented only on a real instantiate; verbatim RED: `Invalid call. Nonexistent function 'warp_room_instantiate_count (via call)' in base 'Node (GameRoot)'.` (0/1 passed, 2/3 asserts). After the fix, confirmed both pre-existing retry tests stay green in isolation: `test_pause_overlay_retry_button_restarts_the_active_level_fresh` (I11, 10/10 asserts) and `test_pause_overlay_retry_does_not_strand_the_player_at_the_hub` (I12, 8/8 asserts) — I12's assertion of retry's player-facing outcome is unaffected since the FSM/state-name/HUD-visibility contract is untouched, only the wasted mid-transition render is skipped. No production call site of `_render_warp_room` exists outside `game_root.gd` itself; grepped `_on_pause_retry_requested` (one caller, the pause overlay's `retry_requested` signal, unaffected) and `warp_room_instantiate_count`/`_suppress_next_warp_room_render` (new, no other references). Full suite: GUT 442/442 (4,213 asserts, +2 tests over the 440/4,202 post-I16 baseline) on 7 of 8 full runs; 1 run showed a single non-reproducing engine-level failure (`Condition "!F" is true. Continuing.`, no named GDScript assertion or test file, ~1-in-6 rate) matching the pre-documented N2 renderer/teardown flake, not a regression — 7 subsequent full runs were clean. Python 63/63, all 4 lints EXIT=0, `git diff --check` clean. Files changed: `src/core/game_root.gd`, `src/gameplay/hub/warp_room.gd`, `tests/integration/test_main_boot.gd`, `tests/integration/test_warp_room.gd`. |
| C19 | `src/core/game_root.gd` | the load poll is unbounded with no timeout and no loading affordance | OPEN | |
| F16 | `src/gameplay/run/level_session.gd` | `respawn_requested` is emitted and never consumed | OPEN | |
| F17 | `tests/gameplay/*` | a test launders `tnt_blast_radius_m`/`mask_stack_maximum` and can go vacuous | OPEN | |
| F18 | `tests/gameplay/*` | no `after_each`; fixture early-returns leak nodes | OPEN | |
| F20 | `src/gameplay/player/player_controller.gd` | `receive_hit()` keeps consuming masks while already dying | FIXED | Verified real by reading `receive_hit()`: it guards on `is_invincible(now_s)` but not on `is_respawning()`, and `respawn()` never touches `_mask_count`/`_invincible_until_s` — only `LevelSession`'s death-recording path (`clear_masks()`) does, later, after the respawn completes. `is_respawning()` becomes true not just from a mask-drained hit but independently from `_physics_process`'s fall-off-level check (`global_position.y < _move_tuning.respawn_floor_y_m`), which schedules a respawn without touching masks at all. Since `ee4b964` wired real tagged hurtbox contacts into `receive_hit()` this session, a hazard the player is still overlapping during the death-delay window (before `respawn()` teleports them away) can fire a second, distinct hit while `is_respawning()` is already true — and the un-guarded code would still decrement `_mask_count` and grant a fresh `mask_hit_invulnerability_s` window for an outcome that was already locked in, permanently losing a mask for nothing. RED first: added `test_receive_hit_does_not_consume_masks_while_already_dying` (`tests/gameplay/test_player_controller.gd`) — grants one mask, calls `request_respawn` directly (simulating an unrelated fall death that never touches masks), then calls `receive_hit` while `is_respawning()` is true; verbatim RED: `[0] expected to equal [1]:  a hit landing while already dying must not consume a mask` (5/6 passed). Fix: added `if is_respawning(): return false` immediately after the existing `is_invincible` guard, before the mask/invincibility-mutating body — mirroring that guard's own early-return shape so a hit landing after the outcome is already decided is a complete no-op (no mask decrement, no invincibility grant, no redundant `request_respawn` call, which was already a no-op via its own `is_respawning()` guard). Grepped every reference to `receive_hit`: the only production caller is `_receive_contact_hit` (`player_controller.gd:217`), which discards the return value, so the added guard changes no caller's control flow; signature unchanged. Full suite green: GUT 437/437 (4,184 asserts, +1 test / +2 asserts over the 436/4,179 post-C17 baseline), Python 63/63, all 4 lints EXIT=0, `git diff --check` clean. One of six full-suite runs during verification showed a single transient failure (summary only — passing/failing counts, not the specific test name, since the run wasn't captured to a file before it scrolled); the following five full runs were clean, and repeated isolated reruns of both touched test files (`test_player_controller.gd`, `test_mercy_and_masks.gd`) were clean every time, consistent with this session's already-documented box-load flakiness rather than a regression from this fix. Files changed: `src/gameplay/player/player_controller.gd`, `tests/gameplay/test_player_controller.gd`. |
| H7 | `tests/integration/test_level_scenes.gd` | re-declares crate counts as the literal `40` in three places instead of reading `LevelMeta` | OPEN | |
| H8 | `tests/integration/test_level_scenes.gd` | the enemy-free assertion is vacuous — nothing joins the `enemy` group (re-introduces Phase 0 M8) | OPEN | |
| H9 | `scenes/segments/*` | only 1 of 3 enemy-bearing segments carries the "ENEMIES IN TASK 17" disclosure | OPEN | |
| H10 | `scenes/levels/wr1_n_sanity_beach.tscn` | §5's "TNT beside a stack" authored as a flat row → 2.0 m blast reaches 1 of 2 neighbours; test written to geometry not the design beat | OPEN | |
| A12 | `README.md:66` | still advertises the retired "Phase 1 content vocabulary tripwire" | OPEN | |
| A13 | `src/core/scalar_math.gd` | open numeric-laundering channel into `src/gameplay/**` the lint's default scope cannot see (no instance yet) | OPEN | |
| D10 | `src/gameplay/crates/crate_logic.gd` | `_finish_break`'s guard untested; `_set_crate_visual` re-arms crates by poking private fields | FIXED | Verified real by reading the code, with one correction: `_finish_break` and `_set_crate_visual` are not in `crate_logic.gd` (a pure static-logic `RefCounted`, no such functions) — they live in `src/gameplay/crates/breakable_crate.gd` (`_finish_break`) and `src/gameplay/run/level_session.gd` (`_set_crate_visual`); the audit's file column is imprecise but the claim itself is real in both locations. (1) `_finish_break`'s own `if _broken: return` guard is real but currently unreachable through the public API — `apply_verb`/`apply_bounce`/`apply_blast`/`_detonate` all already check `_broken` before ever calling it — and grepping every test file confirmed zero coverage calls `_finish_break` directly. (2) `_set_crate_visual` (`level_session.gd:915`) reached into another node's private state via `crate.set("_broken", ...)`, `crate.set("_bounce_count", 0)`, `crate.set("_fuse_active", false)` from outside `BreakableCrate` entirely, used by `_sync_crate_visuals` on both `restore_snapshot` (a freshly-loaded scene, harmless today) and `_record_death`'s relic-mode-only reset (`_sync_crate_visuals(bool(outcome["relic_void_reset"]))`, real per Q4's atomic-relic-attempt design) — confirmed via `record_death`/`_reset_attempt` that `relic_void_reset` is gated to `mode == MODE_RELIC` only, so double-emit is genuinely unreachable today, but the poke bypasses `_finish_break`'s guard and `BreakableCrate`'s own encapsulation entirely, so any future normal-mode caller of this re-arm path (or a refactor that widens `reset_unbroken`) would silently re-arm a bounce crate's full payout with no guard in the way. RED first, two additions: (a) `test_finish_break_guard_blocks_a_second_broken_emission` (`tests/gameplay/test_crate_logic.gd`) calls `_finish_break` directly twice and asserts only one `broken` emission — passed immediately (the guard already works, it's a coverage gap not a live bug), so proved it mutation-sensitive instead: temporarily deleted the guard line in `breakable_crate.gd` (`cp`-backed), re-ran, verbatim RED `ARRAY([5, 9]) != ARRAY([5])`, then restored the file (`git diff --stat` confirmed byte-identical) and confirmed green again. (b) `test_sync_break_state_honestly_marks_broken_and_rearms`, calling the not-yet-existing `sync_break_state` on a real `BreakableCrate`; verbatim RED: `Invalid call. Nonexistent function 'sync_break_state (via call)' in base 'StaticBody3D (BreakableCrate)'`. Fix: added `BreakableCrate.sync_break_state(broken, relic_mode)`, a public method that sets its own `_broken`/`_bounce_count`/`_fuse_active` fields and toggles its own mesh/collision/relic-context — byte-for-byte the same operations `_set_crate_visual` used to poke from outside, just performed by the crate on itself instead of reached into externally; deliberately does not route through `_finish_break` (no signal emission), preserving `_set_crate_visual`'s exact prior observable behavior rather than introducing a new one. `level_session.gd`'s `_set_crate_visual` now just forwards to it. The new test proves the re-arm is genuine, not a flag flip: after re-arming a fully-broken bounce crate, its next bounce pays out and behaves identically to a brand-new crate's first bounce, proving `_bounce_count` was really cleared. Grepped every reference to `_set_crate_visual` (only its own two internal call sites in `_sync_crate_visuals`, unchanged) and `sync_break_state` (new, one caller). Full suite green: GUT 439/439 (4,200 asserts, +2 tests / +30 asserts over the 437/4,184 post-F20 baseline), Python 63/63, all 4 lints EXIT=0, `git diff --check` clean. Files changed: `src/gameplay/crates/breakable_crate.gd`, `src/gameplay/run/level_session.gd`, `tests/gameplay/test_crate_logic.gd`. |
| D11 | `src/gameplay/crates/crate_logic.gd` | bounce `> max` and `<= 0` branches unreachable and untested | FIXED (removed as dead code) | Verified real by reading `bounce_step` and its sole caller: `BreakableCrate.apply_bounce` (`breakable_crate.gd:111-112`) always does `_bounce_count += 1` immediately before calling `bounce_step(_bounce_count, _economy)`, so the value passed is never `<= 0` (starts at 0, always incremented first — including after `D10`'s new `sync_break_state` re-arm, which also resets it to exactly `0`, not negative). And once `bounce_step` reports `breaks=true` at `bounce_count == max_bounces`, `_finish_break` sets `_broken=true`, and `apply_bounce`'s own entry guard (`if _broken or ...: return`) stops any further call — so `bounce_count` can never exceed `max_bounces` either. Grepped every reference to `bounce_step` repo-wide: exactly one production caller (as above) and one existing test (`test_bounce_crate_pays_each_bounce_and_breaks_at_tuned_limit`), which itself mirrors the real caller's increment-first pattern and only ever exercises `bounce_count` in `[1, max_bounces]` — never the two guarded branches. Decision, per the brief's explicit instruction not to inflate coverage by calling a function with inputs no real caller produces: `crate_logic.gd`'s static functions are not a general-purpose library with untrusted external callers — every one of them (`tnt_contact_result`, `blast_crate_ids`, `time_bonus_s`, etc.) is a pure-logic extraction of exactly one `BreakableCrate` call site, decomposed only so it can be tested headless, not a defended public contract. Given the sole caller's own guard structure makes `bounce_count <= 0` and `bounce_count > max_bounces` provably unreachable with no plausible future caller shape that would change that (the only other mutator of `_bounce_count`, `sync_break_state`, resets to exactly `0`), the correct fix is removal, not a contrived direct-call test exercising values the design never produces. Fix: deleted both branches, leaving the single always-taken return (`wumpa`, `breaks: bounce_count >= max_bounces` — the off-by-one at exactly `max_bounces` the audit already confirmed correct), with a comment recording the invariant and pointing back to this row. No new test needed or added — the existing `test_bounce_crate_pays_each_bounce_and_breaks_at_tuned_limit` already covers the entire remaining function body; re-ran it standalone (19/19 in `test_crate_logic.gd`, 209 asserts, unchanged from pre-removal) as direct evidence no coverage was lost. Full suite green: GUT 439/439 (4,200 asserts, unchanged from the post-D10 baseline — no tests added or removed), Python 63/63, all 4 lints EXIT=0, `git diff --check` clean. Files changed: `src/gameplay/crates/crate_logic.gd`. |
| D13 | design vs canon | landing on a standard crate does nothing — code matches plan; plan may not match canon (operator call) | OPEN | |
| I15 | `src/gameplay/hub/warp_room.gd` (whole file) | **⚠ ABOVE P3 — re-derived, not from the original audit.** The hub never actually pauses. `LevelSession` sets `process_mode = PROCESS_MODE_PAUSABLE` (`level_session.gd:66`); `WarpRoom` sets nothing, so it inherits `PROCESS_MODE_ALWAYS` from `GameRoot` (`game_root.gd:97`). Opening the hub Level List sets `get_tree().paused = true` but the warp-room Player, InputRouter, GamepadInput and TouchControls keep running under the modal. Compounding: `_configure_player()` (`warp_room.gd:89`) excludes only `[$UI/LevelList]` — the small hub button — never the full-screen overlay `GameRoot` draws over it, so a tap aimed at the overlay doubles as hub movement/jump. Distinct from C18 (which is only about the debug drawer missing from an otherwise-working list). | VERIFIED | Confirmed by reading: `grep -n process_mode` returns `level_session.gd:66` and `game_root.gd:97` and **nothing** in `warp_room.gd`; exclusion list at `warp_room.gd:89` is literally `[$UI/LevelList]`. Re-deriving agent additionally probed it headless: `WarpRoom.process_mode=0`, `Player.can_process()==true` and `TouchControls.can_process()==true` while `get_tree().paused==true`; the same probe on a real level's `LevelSession` correctly returned `false`. |
| I16 | `src/ui/hud.gd:53-69` (`_input`) | A touch holding Pause can get permanently stuck and later hijack an unrelated touch's release into a spurious pause. `_pause_touch_index` is reset only inside `_input()`, which early-returns when `not visible`. If the HUD is hidden mid-touch (level completes → `_sync_ui_visibility()` sets `_hud.visible = false` for RESULTS), the release is swallowed and the index never clears. Android reuses touch indices after a lift, so this is reachable on the real device. | FIXED | Confirmed real by reading `_input()` (`hud.gd:53-69`) and `GameRoot._sync_ui_visibility()` (`game_root.gd:1056`, sets `_hud.visible = flow.state == GameFlow.State.LEVEL`, i.e. false for RESULTS). RED first: added `test_hud_pause_touch_index_clears_when_hud_is_hidden_mid_touch` (`tests/gameplay/test_input_adapters.gd`) — presses the real Pause button (index 7) on a real `hud.tscn`, hides the HUD (simulating the RESULTS transition), releases (swallowed), re-shows, then presses+releases an unrelated touch at a far position reusing index 7; verbatim RED: `Expected object HUD:<Control#804501066592>(hud.gd) to NOT emit signal [pause_requested]: a pause touch abandoned while the HUD was hidden must not hijack an unrelated touch's release once the index is reused` (440 tests, 439 passing / 1 failing, 4201/4202 asserts). Fix: connected `visibility_changed` in `_ready()` to a new `_on_visibility_changed()` that resets `_pause_touch_index = -1` whenever the HUD becomes hidden, for any reason — so an abandoned touch's index can never survive a hide/show cycle to hijack a later, unrelated touch that reuses it. No signature changed (new private signal connection + private handler only); grepped `_pause_touch_index` — all 4 remaining references are within `hud.gd` itself, no external caller. Full suite green: GUT 440/440 (4,202 asserts, +1 test/+2 asserts over the 439/4,200 baseline), Python 63/63, all 4 lints EXIT=0, `git diff --check` clean. Files changed: `src/ui/hud.gd`, `tests/gameplay/test_input_adapters.gd`. |
| I17 | `src/tuning/tuning_service.gd:189-201` × `src/ui/touch_control_layout.gd:35-46` | `stick_region_width_ratio` and `jump_catchall_width_ratio` are each bounded to `(0.0, 1.0]` but nothing bounds their **sum**, though both are carved from the same `safe_rect.size.x`. In the overlap the player can only ever jump, never start a movement drag. **Same shape as N1** — per-field bounds passing while a cross-field invariant silently does not hold. | VERIFIED | Re-deriving agent probed it: real authored `input.tres` (`stick=0.42`) with `jump_catchall` raised to `0.7` (inside its own bound) → `catalog_is_usable()` still accepts; `TouchControlLayout.calculate()` yields a 124,416 px² overlap; a live `TouchControls` fed a touch at the overlap centre resolved JUMP and `stick_touch_index()` stayed `-1`. |
| I18 | `src/gameplay/hub/warp_room.gd:160-164` | Portal labels fall back to blank text with no guard and no test. `label.text = String(portal.get_meta("display_name", portal_id))` relies on every portal instance overriding the metadata; the base `scenes/props/portal.tscn` already carries `metadata/display_name = ""`, so the key *exists* with an empty value and `get_meta`'s default never fires — a future portal that omits the override renders a blank label in the 3D world. | VERIFIED | Re-deriving agent grepped `tests/integration/test_warp_room.gd` and `test_main_boot.gd` for `display_name`/`Label3D`: zero hits. No test reads rendered label text. |
| I19 | `src/ui/results_screen.gd:69-87` (`_missed_segments_text`) | The results screen prints raw GDScript array syntax on any non-perfect run: `"%s: %s" % [segment_name, str(grouped[segment_name])]` renders as `Beach Landing: [3, 7, 12]`. I10's new test only covers the empty case (`MISSED CRATES  NONE`), so nothing catches the path that fires on essentially every real completion. | VERIFIED | Read `results_screen.gd:69-87` directly — `str(grouped[segment_name])` on an Array is confirmed verbatim. |
| I20 | — | No recoverable claim. The audit recorded only "`I15`–`I21` hub/UI minors" and the raw auditor notes are gone (searched `docs/`, `handover/`, `/tmp`). A fresh read-only re-derivation of the whole hub/UI lane found **five** real findings, filed above as I15–I19, and explicitly declined to pad to seven. | NOT APPLICABLE | Detail unrecoverable; lane re-audited from scratch instead. Closing this as a phantom row rather than inventing a claim to fill it. |
| I21 | — | No recoverable claim — see I20. | NOT APPLICABLE | Detail unrecoverable; lane re-audited from scratch instead. |

## Final

| ID | Item | Status | Evidence |
|----|------|--------|----------|
| CKA | Checkpoint A verification + handback (full suite green count, APK SHA-256, handback doc) | OPEN | |

---

## Notes

- `I15`–`I21` and `G8`–`G14` are collapsed ranges in the audit's P3 prose. Their
  individual claims must be recovered from the raw auditor notes or re-derived by
  reading the code before they can leave `OPEN`.
- Anything a fix round raises that is new goes in as a new row, not a footnote.
</content>
</invoke>
